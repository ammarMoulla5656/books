# ⚡ اختبارات الأداء | Performance Tests

هذا المجلد يحتوي على أدوات اختبار الحمل والأداء للموقع.

---

## 📋 الملفات المتاحة

### 1. `quick_test.py` - اختبار سريع
اختبار سريع ومباشر مع عدد مستخدمين قابل للتعديل.

```bash
python tests/quick_test.py 100
```

### 2. `load_test.py` - اختبار شامل
اختبار مفصل مع تفاصيل كاملة وإحصائيات دقيقة.

```bash
python tests/load_test.py
```

### 3. `stress_test.py` - اختبار ضغط شديد
اختبار أقصى حمل ممكن - بدون قيود!

```bash
python tests/stress_test.py 1000
```

---

## 🚀 كيف تستخدمها؟

### 1. تثبيت المتطلبات

```bash
pip install requests
```

### 2. تشغيل الموقع

```bash
npm run dev
# أو للإنتاج (أفضل):
npm run build && npm start
```

### 3. تشغيل الاختبار

```bash
# اختبار سريع
python tests/quick_test.py 50

# اختبار شامل
python tests/load_test.py

# اختبار ضغط شديد
python tests/stress_test.py 1000
```

---

## 📊 فهم النتائج

### نسبة النجاح
- ✅ **> 95%**: ممتاز
- ⚠️ **90-95%**: جيد
- ❌ **< 90%**: يحتاج تحسين

### وقت الاستجابة
- ✅ **< 0.5s**: ممتاز
- ⚠️ **0.5-1s**: جيد
- ❌ **> 1s**: بطيء

### معدل الطلبات
- ✅ **> 500 req/s**: ممتاز
- ⚠️ **200-500 req/s**: جيد
- ❌ **< 200 req/s**: ضعيف

---

## 💡 نصائح

1. **استخدم Production Build** للحصول على نتائج دقيقة
2. **ابدأ بأعداد صغيرة** ثم زد تدريجياً
3. **راقب استهلاك الموارد** (CPU, RAM)
4. **لا تختبر مواقع حقيقية** بدون إذن!

---

[← الرجوع إلى الوثائق](../docs/)
[← الرجوع إلى الصفحة الرئيسية](../README.md)
