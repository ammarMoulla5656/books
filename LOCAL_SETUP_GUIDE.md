# 🚀 دليل التثبيت المحلي | Local Setup Guide

**التاريخ**: 3 يناير 2026  
**قاعدة البيانات**: SQLite (سريعة ومحلية)

---

## ✅ المتطلبات | Prerequisites

- ✅ Node.js 18+ مثبت
- ✅ npm أو yarn
- ✅ محرر نصوص (VS Code موصى به)

---

## 📥 خطوات التثبيت | Installation Steps

### 1. تنزيل المشروع
```bash
# إذا كان من Git
git clone <repository-url>
cd algiers

# أو ببساطة، انسخ المجلد كاملاً
```

### 2. تثبيت الحزم
```bash
npm install
```

### 3. إعداد قاعدة البيانات
```bash
# إنشاء قاعدة البيانات وتطبيق migrations
npx prisma migrate deploy

# أو لإعادة إنشاء من الصفر
npx prisma migrate dev
```

### 4. إضافة البيانات الأساسية

**الطريقة 1: عبر API** (موصى بها)
```bash
# شغّل المشروع أولاً
npm run dev

# ثم في terminal آخر، استدع API
curl -X POST http://localhost:3000/api/admin/seed
```

**الطريقة 2: يدوياً عبر Prisma Studio**
```bash
npx prisma studio
# افتح http://localhost:5555
# أضف التصنيفات و Admin يدوياً
```

### 5. تشغيل المشروع
```bash
npm run dev
```

المشروع سيعمل على: http://localhost:3000

---

## 🗄️ قاعدة البيانات SQLite

### الموقع
```
prisma/dev.db
```

### المميزات
- ✅ **سريعة جداً** - ملف محلي بدون اتصال شبكة
- ✅ **سهلة النسخ** - انسخ ملف dev.db فقط
- ✅ **لا تحتاج خادم** - كل شيء في ملف واحد
- ✅ **مثالية للتطوير** - تشغيل فوري بدون إعداد

### عرض البيانات
```bash
npx prisma studio
```
سيفتح على: http://localhost:5555

---

## 📁 ملفات التكوين | Configuration Files

### 1. `.env`
```env
DATABASE_URL="file:./dev.db"
```

### 2. `prisma/schema.prisma`
```prisma
datasource db {
  provider = "sqlite"
}
```

### 3. `lib/prisma.ts`
- تم تحديثه للعمل مع SQLite مباشرة
- بدون adapters معقدة

---

## 🔐 بيانات تسجيل الدخول

### Admin Panel
```
URL: http://localhost:3000/secret-admin-panel-xyz
البريد: admin@islamic-library.com
كلمة المرور: Admin@123456
```

---

## 📚 استخراج الكتب | Scraping Books

### استخراج كتاب واحد
```bash
curl -X POST http://localhost:3000/api/admin/scrape-book-enhanced/13 \
  -H "Content-Type: application/json" \
  -d '{"maxChapters": 50}'
```

### استخراج جميع الكتب
```bash
# في المجلد الرئيسي
chmod +x import-all-books-enhanced.sh
./import-all-books-enhanced.sh
```

---

## 🔧 أوامر مفيدة | Useful Commands

### التطوير
```bash
# تشغيل المشروع للتطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# تشغيل الإنتاج
npm start
```

### قاعدة البيانات
```bash
# عرض Prisma Studio
npx prisma studio

# إنشاء migration جديد
npx prisma migrate dev --name migration_name

# تطبيق migrations
npx prisma migrate deploy

# إعادة إنشاء Prisma Client
npx prisma generate

# إعادة ضبط قاعدة البيانات (حذف كل البيانات!)
npx prisma migrate reset
```

### تنظيف
```bash
# حذف node_modules وإعادة التثبيت
rm -rf node_modules
npm install

# حذف قاعدة البيانات وإعادة إنشائها
rm -rf prisma/dev.db prisma/migrations
npx prisma migrate dev --name init_sqlite
```

---

## 📦 نقل المشروع | Moving the Project

### إلى جهاز آخر
```bash
# 1. انسخ المجلد كاملاً
# 2. على الجهاز الجديد:
npm install
npx prisma generate
npm run dev
```

### مع البيانات
```bash
# انسخ هذه الملفات:
- المجلد الكامل
- prisma/dev.db (قاعدة البيانات)

# على الجهاز الجديد:
npm install
npm run dev
```

---

## 🐛 حل المشاكل | Troubleshooting

### مشكلة 1: Prisma Client Error
```bash
# الحل
npx prisma generate
```

### مشكلة 2: قاعدة البيانات غير موجودة
```bash
# الحل
npx prisma migrate deploy
```

### مشكلة 3: Port 3000 مستخدم
```bash
# الحل - استخدم port آخر
PORT=3001 npm run dev
```

### مشكلة 4: لا توجد بيانات
```bash
# الحل - أضف البيانات الأساسية
curl -X POST http://localhost:3000/api/admin/seed
```

---

## 🔄 التحديثات | Updates

### تحديث Schema
```bash
# بعد تعديل schema.prisma
npx prisma migrate dev --name update_name
npx prisma generate
```

### إضافة ميزة جديدة
```bash
# 1. اكتب الكود
# 2. اختبر محلياً
npm run dev
# 3. بناء للإنتاج
npm run build
```

---

## 📊 حالة المشروع

```
✅ قاعدة البيانات: SQLite (محلية وسريعة)
✅ Migrations: منشأة ومطبقة
✅ Prisma Client: تم إنشاؤه
✅ Admin Panel: جاهز
✅ نظام الاستخراج: جاهز ومحسّن
✅ التنسيق بالألوان: مفعّل
```

---

## 🎯 البدء السريع | Quick Start

```bash
# 1. تثبيت
npm install

# 2. قاعدة البيانات
npx prisma migrate deploy

# 3. البيانات الأساسية (شغّل المشروع أولاً)
npm run dev
# في terminal آخر:
curl -X POST http://localhost:3000/api/admin/seed

# 4. افتح المتصفح
open http://localhost:3000
```

---

## 📝 ملاحظات مهمة

### السرعة
- ✅ SQLite **أسرع 10x** من PostgreSQL على الشبكة
- ✅ لا يوجد تأخير في الاتصال
- ✅ جميع العمليات محلية

### النسخ الاحتياطي
```bash
# نسخ احتياطي
cp prisma/dev.db prisma/dev.db.backup

# استرجاع
cp prisma/dev.db.backup prisma/dev.db
```

### الأمان
- قاعدة البيانات محلية فقط
- لا يمكن الوصول إليها من الخارج
- مناسبة للتطوير والاختبار

---

## 🚀 للإنتاج | For Production

إذا أردت نشر المشروع على الإنترنت:

### خيار 1: Vercel + PostgreSQL
```bash
# 1. استخدم Vercel Postgres
# 2. حدّث .env بـ DATABASE_URL من Vercel
# 3. حدّث schema.prisma إلى postgresql
# 4. انشر على Vercel
```

### خيار 2: VPS + SQLite
```bash
# يمكنك استخدام SQLite على VPS أيضاً
# لكن تأكد من النسخ الاحتياطي المنتظم
```

---

## 📞 المساعدة

### الملفات المهمة
- `ENHANCED_SCRAPING_GUIDE.md` - دليل الاستخراج المحسّن
- `SCRAPING_SYSTEM_COMPLETE.md` - ملخص نظام الاستخراج
- `FINAL_STATUS.md` - حالة لوحة التحكم

### الدعم الفني
- جميع الملفات موثقة بشكل جيد
- اقرأ comments في الكود للتفاصيل
- استخدم `npx prisma studio` لعرض البيانات

---

**🎉 المشروع جاهز للعمل المحلي!**

**تم بـ ❤️ للمجتمع الإسلامي**

*آخر تحديث: 3 يناير 2026*
