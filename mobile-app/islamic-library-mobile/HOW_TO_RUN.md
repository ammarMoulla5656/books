# 🚀 كيفية تشغيل التطبيق

## ✅ الحل السريع (مع Terminal مرئي)

لديك الآن **3 طرق** لتشغيل التطبيق مع ظهور Terminal:

### 🎯 الطريقة 1: ملف BAT (موصى به)

**Double-click على أحد هذه الملفات:**

#### 📱 `start.bat`
- فتح في نفس النافذة
- يظهر لك Terminal
- يبقى مفتوح حتى تغلقه

#### 🪟 `start-new-window.bat`
- فتح في نافذة جديدة
- يعمل في الخلفية
- يمكنك الاستمرار في العمل

**كيف؟**
1. افتح مجلد المشروع `islamic-library-mobile`
2. Double-click على `start.bat` أو `start-new-window.bat`
3. انتظر قليلاً... سيفتح Expo!

---

### 🎨 الطريقة 2: PowerShell Script

**إذا كنت تفضل PowerShell:**

1. Right-click على `start.ps1`
2. اختر **"Run with PowerShell"**

⚠️ **إذا ظهر خطأ Execution Policy:**
```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

---

### 💻 الطريقة 3: من VSCode Terminal

**داخل VSCode:**

1. اضغط `` Ctrl + ` `` لفتح Terminal
2. اكتب:
```bash
npm start
```

---

## 📱 بعد التشغيل

سترى شاشة Expo مع خيارات:

```
› Press a │ open Android
› Press i │ open iOS simulator
› Press w │ open web

› Press j │ open debugger
› Press r │ reload app
› Press m │ toggle menu
```

### للتشغيل على الهاتف:
1. **ثبّت تطبيق Expo Go** على هاتفك:
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)

2. **امسح QR Code** الذي يظهر في Terminal

3. **🎉 التطبيق سيفتح على هاتفك!**

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "expo is not recognized"
**الحل:** تم إصلاحها! استخدم `npm start` أو الملفات الجاهزة

### المشكلة: Port 8081 مستخدم
**الحل:**
```bash
npx expo start --port 8082
```

### المشكلة: الهاتف لا يتصل
**الحل:**
- تأكد أن الهاتف والكمبيوتر على نفس الـ WiFi
- جرب Tunnel mode:
```bash
npx expo start --tunnel
```

### المشكلة: التطبيق لا يفتح
**الحل:**
```bash
# مسح الـ cache
npx expo start -c
```

---

## 🎯 الطرق المختصرة

بعد التشغيل، يمكنك استخدام:

| المفتاح | الوظيفة |
|---------|---------|
| `a` | فتح على Android Emulator |
| `i` | فتح على iOS Simulator (Mac) |
| `w` | فتح في المتصفح |
| `r` | إعادة تحميل التطبيق |
| `m` | فتح Dev Menu |
| `?` | إظهار المساعدة |

---

## 📚 ملاحظات إضافية

### التشغيل على Android Emulator
1. افتح Android Studio
2. شغّل Emulator
3. في Terminal، اضغط `a`

### التشغيل على iOS Simulator (Mac فقط)
1. تأكد من تثبيت Xcode
2. في Terminal، اضغط `i`

### Hot Reload
التطبيق يدعم **Hot Reload** - أي تغيير في الكود سيظهر مباشرة بدون إعادة تشغيل!

---

## 🆘 محتاج مساعدة؟

إذا واجهت أي مشكلة:

1. تأكد من تثبيت Node.js:
```bash
node --version  # يجب أن يظهر v18+
npm --version   # يجب أن يظهر v8+
```

2. أعد تثبيت المكتبات:
```bash
npm install
```

3. امسح الـ cache:
```bash
npx expo start -c
```

4. إذا استمرت المشكلة، احذف `node_modules` وأعد التثبيت:
```bash
rmdir /s /q node_modules
npm install
```

---

## ✨ نصائح للتطوير

1. **استخدم Fast Refresh** - التغييرات تظهر فوراً
2. **افتح Dev Menu** - هز الهاتف أو اضغط `m`
3. **استخدم Debugger** - اضغط `j` لفتح Chrome DevTools
4. **تابع الـ Console** - الأخطاء تظهر في Terminal

---

**🎉 مبروك! التطبيق جاهز للتشغيل!**

---

_للمزيد من التفاصيل، اقرأ [README.md](./README.md)_
