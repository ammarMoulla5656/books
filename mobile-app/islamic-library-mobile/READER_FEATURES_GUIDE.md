# 📖 دليل ميزات القارئ | Reader Features Guide

## 📚 نظرة عامة

دليل شامل لميزات القارئ المتقدمة: العلامات المرجعية (Bookmarks)، التظليل (Highlights)، والملاحظات (Notes).

**الحالة:** ✅ مكتمل بالكامل
**الملفات:** 4 (BookReaderScreen + 3 Components)
**الأسطر:** ~1,400+

---

## 🎯 الميزات الرئيسية

### 1️⃣ العلامات المرجعية (Bookmarks)

**الوصف:** حفظ الصفحات المهمة للرجوع إليها لاحقاً

**المكونات:**
- `BookmarksModal.tsx` (~150 lines) - مودال عرض وإدارة العلامات

**الوظائف:**
```typescript
// إضافة علامة مرجعية
addBookmark({
  bookId: string,
  page: number,
  title: string
});

// عرض العلامات المرجعية
const bookmarks = getBookBookmarks(bookId);

// الانتقال إلى علامة مرجعية
handleBookmarkPress(bookmark) => setCurrentPage(bookmark.page);

// حذف علامة مرجعية
removeBookmark(bookmarkId);
```

**الواجهة:**
- زر في شريط الأدوات العلوي (أيقونة `bookmarks`)
- FAB (Floating Action Button) باللون الأخضر
- مودال يعرض جميع العلامات المحفوظة
- كل علامة تحتوي على: العنوان، رقم الصفحة، زر حذف
- حالة فارغة مع رسالة توضيحية

**التخزين:**
- يتم حفظ العلامات في `bookStore` مع Zustand
- Persistent storage مع AsyncStorage
- يتم تحديث القائمة تلقائياً عند الإضافة/الحذف

---

### 2️⃣ التظليل (Highlights)

**الوصف:** تظليل النصوص المهمة بألوان مختلفة

**المكونات:**
- `HighlightColorPicker.tsx` (~140 lines) - مودال اختيار لون التظليل

**الألوان المتاحة:**
```typescript
const HIGHLIGHT_COLORS = [
  { color: '#FFEB3B', name: 'أصفر' },
  { color: '#4CAF50', name: 'أخضر' },
  { color: '#2196F3', name: 'أزرق' },
  { color: '#FF9800', name: 'برتقالي' },
  { color: '#E91E63', name: 'وردي' },
  { color: '#9C27B0', name: 'بنفسجي' },
];
```

**الوظائف:**
```typescript
// إضافة تظليل
addHighlight({
  bookId: string,
  page: number,
  text: string,
  color: string
});

// عرض التظليلات
const highlights = getBookHighlights(bookId);

// حذف تظليل
removeHighlight(highlightId);
```

**الواجهة:**
- FAB باللون الأصفر (أيقونة `color-fill`)
- يفتح مودال اختيار اللون
- معاينة للنص المحدد
- 6 خيارات ألوان مع الأسماء
- تأكيد بعد الحفظ

**ملاحظة التطوير:**
حالياً يتم محاكاة تحديد النص. في التطبيق الحقيقي:
- استخدام React Native Text Selection API
- أو مكتبة مثل `react-native-selectable-text`
- تتبع موضع التظليل على الصفحة

---

### 3️⃣ الملاحظات (Notes)

**الوصف:** إضافة ملاحظات شخصية لكل صفحة

**المكونات:**
- `NoteModal.tsx` (~180 lines) - مودال إضافة وتحرير الملاحظات

**الوظائف:**
```typescript
// إضافة ملاحظة جديدة
addNote({
  bookId: string,
  page: number,
  content: string
});

// تحديث ملاحظة موجودة
updateNote(noteId, content);

// حذف ملاحظة
removeNote(noteId);

// عرض ملاحظات الصفحة
const pageNotes = getBookNotes(bookId).filter(
  note => note.page === currentPage
);
```

**الواجهة:**
- FAB بلون أخضر (أيقونة `create-outline`)
- مودال مع TextInput متعدد الأسطر
- عداد الأحرف
- أزرار: حفظ، إلغاء، حذف (للملاحظات الموجودة)
- عرض الملاحظات أسفل محتوى الصفحة
- تصميم بطاقة صفراء (sticky note style)

**الميزات الإضافية:**
- تحرير الملاحظات بالضغط عليها
- عرض تاريخ آخر تعديل
- KeyboardAvoidingView للتوافق مع iOS/Android
- تحديث فوري للقائمة بعد الإضافة/التعديل/الحذف

---

## 📊 تتبع القراءة (Reading Tracking)

### وقت القراءة (Reading Time)

**الوظيفة:**
```typescript
// بدء تتبع الوقت
const [readingStartTime, setReadingStartTime] = useState(Date.now());

// حفظ الوقت عند الخروج
useEffect(() => {
  return () => {
    const duration = Math.floor((Date.now() - readingStartTime) / 1000);
    addReadingHistory({
      bookId,
      page: currentPage,
      duration
    });
  };
}, []);
```

**البيانات المحفوظة:**
```typescript
interface ReadingHistoryEntry {
  bookId: string;
  page: number;
  timestamp: string;
  duration: number; // seconds
}
```

### تحديث التقدم (Progress Update)

**الوظيفة:**
```typescript
// حساب التقدم
const progress = Math.round((currentPage / totalPages) * 100);

// تحديث تلقائي عند تغيير الصفحة
useEffect(() => {
  updateProgress(bookId, progress);
}, [progress, bookId]);
```

---

## 🎨 الواجهة (UI/UX)

### شريط الأدوات العلوي (Top Bar)

```
[إغلاق] [عنوان الكتاب - صفحة X من Y] [العلامات المرجعية] [الإعدادات]
```

**الأزرار:**
- `close` - العودة للشاشة السابقة (مع حفظ وقت القراءة)
- `bookmarks` - فتح مودال العلامات المرجعية
- `settings-outline` - فتح إعدادات القراءة

### شريط التقدم (Progress Bar)

```
[████████░░░░░░░░░░] 42%
```

- ارتفاع: 3px
- لون التعبئة: Primary color
- تحديث تلقائي مع تغيير الصفحة

### أزرار الإجراءات العائمة (FABs)

موقع: أسفل اليسار، فوق شريط التنقل

```
[📝] إضافة ملاحظة (أخضر)
[🎨] تظليل نص (أصفر)
[🔖] إضافة علامة (أزرق)
```

**الخصائص:**
- حجم: 56x56
- Shadow/Elevation لإبراز الأزرار
- تأثير Pressed عند الضغط

### شريط التنقل السفلي (Bottom Navigation)

```
[◀ السابق] [42] [التالي ▶]
```

- يظهر عند الضغط على المحتوى
- Previous/Next مع Disabled state
- عرض رقم الصفحة الحالية

### عرض الملاحظات (Notes Display)

```
┌─────────────────────────────┐
│ 📝 ملاحظات هذه الصفحة (2)  │
├─────────────────────────────┤
│ [ملاحظة 1...]              │
│ 2024-01-27                  │
├─────────────────────────────┤
│ [ملاحظة 2...]              │
│ 2024-01-26                  │
└─────────────────────────────┘
```

---

## 🗂️ هيكل الملفات

```
src/
├── components/
│   ├── BookmarksModal.tsx      # مودال العلامات المرجعية
│   ├── HighlightColorPicker.tsx # مودال اختيار اللون
│   ├── NoteModal.tsx            # مودال الملاحظات
│   └── index.ts                 # تصدير المكونات
│
├── screens/main/
│   └── BookReaderScreen.tsx     # شاشة القارئ (محدثة)
│
└── stores/
    └── bookStore.ts              # Store مع الوظائف
```

---

## 🔄 تدفق البيانات (Data Flow)

### Bookmarks Flow

```
[User Action] ─> [handleAddBookmark]
                       ↓
                  [addBookmark (Store)]
                       ↓
                  [Zustand State Update]
                       ↓
                  [AsyncStorage Persist]
                       ↓
                  [Success Alert]
```

### Highlights Flow

```
[Text Selection] ─> [handleTextSelect]
                          ↓
                   [Show Color Picker]
                          ↓
                   [User Selects Color]
                          ↓
                   [addHighlight (Store)]
                          ↓
                   [Persist to Storage]
                          ↓
                   [Success Alert]
```

### Notes Flow

```
[FAB Press] ─> [Show Note Modal]
                     ↓
              [User Enters Text]
                     ↓
              [handleSaveNote]
                     ↓
      ┌──────────────┴──────────────┐
      │                             │
 [addNote (New)]          [updateNote (Edit)]
      │                             │
      └──────────────┬──────────────┘
                     ↓
              [Zustand Update]
                     ↓
              [AsyncStorage Persist]
                     ↓
              [Refresh Page Notes]
                     ↓
              [Close Modal]
```

---

## 📝 أمثلة الاستخدام

### مثال 1: إضافة علامة مرجعية

```typescript
// في BookReaderScreen
const handleAddBookmark = () => {
  addBookmark({
    bookId: 'book_1',
    page: 42,
    title: 'صفحة 42'
  });
  Alert.alert('تم الحفظ', 'تمت إضافة العلامة المرجعية بنجاح');
};
```

### مثال 2: تظليل نص

```typescript
// اختيار نص (محاكاة)
const handleTextSelect = () => {
  const mockSelectedText = 'فإن العلم الشرعي هو أساس كل خير';
  setSelectedText(mockSelectedText);
  setShowHighlightPicker(true);
};

// اختيار اللون
const handleHighlightColorSelect = (color: string) => {
  if (selectedText) {
    addHighlight({
      bookId: 'book_1',
      page: 42,
      text: selectedText,
      color: '#FFEB3B'
    });
    setSelectedText('');
    Alert.alert('تم التظليل', 'تم تظليل النص بنجاح');
  }
};
```

### مثال 3: إضافة ملاحظة

```typescript
// فتح المودال
const handleAddNote = () => {
  setCurrentNote(null);
  setShowNoteModal(true);
};

// حفظ الملاحظة
const handleSaveNote = (content: string) => {
  if (currentNote) {
    // تحديث
    updateNote(currentNote.id, content);
  } else {
    // إضافة جديدة
    addNote({
      bookId: 'book_1',
      page: 42,
      content: 'ملاحظة مهمة حول هذا الموضوع...'
    });
  }
  // تحديث القائمة
  const notes = getBookNotes('book_1').filter(
    note => note.page === 42
  );
  setPageNotes(notes);
};
```

---

## 🎨 التصميم (Styling)

### الألوان

```typescript
// FABs
Bookmark:   Colors.light.primary  (#1A472A)
Highlight:  #FFEB3B (Yellow)
Note:       Colors.light.success  (Green)

// Note Cards
Background: #FFF9C4 (Light Yellow)
Border:     #FBC02D (Dark Yellow)

// Highlights (6 colors)
Yellow:     #FFEB3B
Green:      #4CAF50
Blue:       #2196F3
Orange:     #FF9800
Pink:       #E91E63
Purple:     #9C27B0
```

### الأبعاد

```typescript
// FABs
Size: 56x56
Border Radius: 28
Shadow: elevation 8

// Icon Buttons (Top Bar)
Size: 40x40
Border Radius: 20

// Modal Panels
Border Radius: 20 (top corners)
Padding: 20-24px
```

---

## 🔧 التخصيص

### إضافة ألوان تظليل جديدة

```typescript
// في HighlightColorPicker.tsx
const HIGHLIGHT_COLORS = [
  // ... الألوان الموجودة
  { color: '#795548', name: 'بني' }, // لون جديد
];
```

### تخصيص شكل الملاحظات

```typescript
// في BookReaderScreen.tsx styles
noteCard: {
  padding: 16,
  marginBottom: 12,
  borderRadius: 12,
  backgroundColor: '#E3F2FD', // لون جديد
  borderLeftWidth: 4,
  borderLeftColor: '#1976D2', // لون جديد
},
```

### تغيير موقع FABs

```typescript
// في BookReaderScreen.tsx styles
fabContainer: {
  position: 'absolute',
  bottom: 100,    // تغيير الموقع العمودي
  right: 20,      // تغيير من left إلى right
  gap: 12,
},
```

---

## 🐛 التعامل مع الأخطاء

### سيناريوهات الخطأ

1. **فشل حفظ العلامة المرجعية**
```typescript
try {
  addBookmark({ bookId, page, title });
} catch (error) {
  Alert.alert('خطأ', 'فشل حفظ العلامة المرجعية');
}
```

2. **ملاحظة فارغة**
```typescript
const handleSave = () => {
  if (content.trim().length === 0) {
    return; // منع الحفظ
  }
  onSave(content.trim());
};
```

3. **نص تظليل فارغ**
```typescript
const handleHighlightColorSelect = (color: string) => {
  if (!selectedText) {
    Alert.alert('خطأ', 'يجب تحديد نص أولاً');
    return;
  }
  addHighlight({ bookId, page, text: selectedText, color });
};
```

---

## ⚡ الأداء (Performance)

### التحسينات المطبقة

1. **Lazy Loading للمودالات**
```typescript
// المودالات تُعرض فقط عند visible={true}
<BookmarksModal visible={showBookmarks} ... />
```

2. **تحديث جزئي للحالة**
```typescript
// تحديث فقط العلامات عند فتح المودال
useEffect(() => {
  if (showBookmarks) {
    setBookmarks(getBookBookmarks(bookId));
  }
}, [showBookmarks, bookId]);
```

3. **تصفية ذكية للملاحظات**
```typescript
// فقط ملاحظات الصفحة الحالية
const pageNotes = getBookNotes(bookId).filter(
  note => note.page === currentPage
);
```

---

## 🧪 الاختبار (Testing)

### سيناريوهات الاختبار

#### Bookmarks
- ✅ إضافة علامة مرجعية
- ✅ عرض قائمة العلامات
- ✅ الانتقال إلى صفحة من علامة مرجعية
- ✅ حذف علامة مرجعية
- ✅ عرض حالة فارغة

#### Highlights
- ✅ اختيار نص (محاكاة)
- ✅ اختيار لون من 6 ألوان
- ✅ حفظ التظليل
- ✅ عرض النص المحدد في المعاينة

#### Notes
- ✅ إضافة ملاحظة جديدة
- ✅ تحرير ملاحظة موجودة
- ✅ حذف ملاحظة
- ✅ عرض ملاحظات الصفحة
- ✅ عداد الأحرف
- ✅ منع حفظ ملاحظة فارغة

#### Reading Tracking
- ✅ تتبع وقت القراءة
- ✅ حفظ السجل عند الخروج
- ✅ تحديث التقدم مع تغيير الصفحة

---

## 📈 التحسينات المستقبلية

### قريباً (Phase 7)

1. **تحديد نص حقيقي**
   - استخدام React Native Text Selection API
   - عرض التظليلات على النص الفعلي
   - تحديد موقع دقيق على الصفحة

2. **بحث في الملاحظات**
   - بحث عبر جميع ملاحظات الكتاب
   - تصفية حسب التاريخ
   - تصدير الملاحظات

3. **مزامنة سحابية**
   - حفظ على السيرفر
   - مزامنة بين الأجهزة
   - Backup تلقائي

4. **مشاركة**
   - مشاركة علامة مرجعية
   - مشاركة نص مظلل
   - مشاركة ملاحظة

---

## 📊 الإحصائيات

```
الملفات الجديدة:      4
الأسطر المضافة:        ~1,400
المكونات:              3 (BookmarksModal, HighlightColorPicker, NoteModal)
الشاشات المحدثة:       1 (BookReaderScreen)
الميزات:               3 (Bookmarks, Highlights, Notes)
التكامل مع Store:      ✅ كامل
Persistence:           ✅ AsyncStorage
Type Safety:           ✅ TypeScript
```

---

## 🎓 الخلاصة

تم تطبيق ميزات متقدمة للقارئ بنجاح:

✅ **العلامات المرجعية** - حفظ الصفحات المهمة والانتقال السريع
✅ **التظليل** - تحديد النصوص المهمة بـ 6 ألوان
✅ **الملاحظات** - إضافة وتحرير ملاحظات لكل صفحة
✅ **تتبع القراءة** - حفظ وقت القراءة والتقدم
✅ **واجهة سلسة** - FABs + Modals مع تصميم عصري
✅ **أداء محسّن** - Lazy loading + تحديث جزئي
✅ **Type Safety** - TypeScript في جميع المكونات

**التطبيق جاهز لـ Phase 7!** 🚀

---

**مصنوع بـ ❤️ لخدمة المسلمين في كل مكان**

**🇩🇿 Made in Algeria**
