# 📱 خطة تطبيق المكتبة الإسلامية للموبايل - متكاملة وشاملة

> **خطة تفصيلية لبناء تطبيق موبايل متكامل مع الموقع الحالي**
>
> **قاعدة بيانات مشتركة | API موحد | تجربة مستخدم متناسقة**

---

## 📋 جدول المحتويات

1. [نظرة عامة على المشروع](#1-نظرة-عامة-على-المشروع)
2. [تحليل المشروع الحالي](#2-تحليل-المشروع-الحالي)
3. [اختيار التقنيات](#3-اختيار-التقنيات)
4. [هيكلة قاعدة البيانات المشتركة](#4-هيكلة-قاعدة-البيانات-المشتركة)
5. [تصميم الـ API](#5-تصميم-الـ-api)
6. [هيكلة تطبيق الموبايل](#6-هيكلة-تطبيق-الموبايل)
7. [الميزات المطلوبة بالتفصيل](#7-الميزات-المطلوبة-بالتفصيل)
8. [تصميم واجهة المستخدم (UI/UX)](#8-تصميم-واجهة-المستخدم-uiux)
9. [المصادقة والأمان](#9-المصادقة-والأمان)
10. [التخزين المحلي والأوفلاين](#10-التخزين-المحلي-والأوفلاين)
11. [الإشعارات](#11-الإشعارات)
12. [الأداء والتحسينات](#12-الأداء-والتحسينات)
13. [خطة التنفيذ المرحلية](#13-خطة-التنفيذ-المرحلية)
14. [الاختبار](#14-الاختبار)
15. [النشر](#15-النشر)
16. [الصيانة والتحديثات](#16-الصيانة-والتحديثات)
17. [الجدول الزمني](#17-الجدول-الزمني)
18. [الموارد المطلوبة](#18-الموارد-المطلوبة)

---

## 1. نظرة عامة على المشروع

### 🎯 الهدف الرئيسي

بناء تطبيق موبايل متكامل (iOS + Android) للمكتبة الإسلامية يعمل بتناغم تام مع الموقع الحالي، مع قاعدة بيانات مشتركة وتجربة مستخدم موحدة عبر جميع المنصات.

### 🌟 الرؤية

**"توفير تجربة قراءة إسلامية متكاملة في جيب كل مسلم"**

- 📱 تطبيق موبايل سلس وسريع
- 🌐 مزامنة فورية مع الموقع
- 📖 قراءة أوفلاين
- 🔔 إشعارات ذكية
- 🎨 تصميم عربي أصيل

### 📊 المقاييس المستهدفة

| المقياس | الهدف |
|---------|-------|
| وقت تحميل التطبيق | < 2 ثانية |
| استهلاك البطارية | أقل من 5% في الساعة |
| حجم التطبيق | < 50 ميجا |
| تقييم المستخدمين | 4.5+ نجوم |
| معدل الاحتفاظ (Retention) | > 60% بعد 30 يوم |

---

## 2. تحليل المشروع الحالي

### ✅ ما لدينا الآن

#### الموقع (Web)
- ✅ Next.js 16 + React 19
- ✅ PostgreSQL + Prisma
- ✅ نظام مصادقة كامل
- ✅ لوحة تحكم إدارية
- ✅ API Routes جاهز
- ✅ نظام الكتب والفئات
- ✅ العلامات المرجعية والتظليل
- ✅ تحليلات وإحصائيات

#### قاعدة البيانات (PostgreSQL)

**الجداول الحالية:**

```sql
- users (المستخدمين)
- admins (المسؤولين)
- books (الكتب)
- categories (الفئات)
- chapters (الفصول)
- sections (الأقسام)
- bookmarks (العلامات المرجعية)
- highlights (التظليلات)
- visitor_logs (سجل الزوار)
- daily_stats (الإحصائيات اليومية)
```

### 🎯 ما نحتاجه للموبايل

#### إضافات قاعدة البيانات
- ✨ جدول للأجهزة (devices)
- ✨ جدول للإشعارات (notifications)
- ✨ جدول لتتبع القراءة (reading_progress)
- ✨ جدول للمزامنة (sync_queue)
- ✨ جدول للتنزيلات (offline_downloads)

#### إضافات API
- ✨ Authentication بـ JWT
- ✨ Refresh Tokens
- ✨ Device Registration
- ✨ Push Notifications
- ✨ Sync Endpoints

---

## 3. اختيار التقنيات

### 🎨 التقنية المقترحة: **React Native + Expo**

#### ✅ لماذا React Native + Expo؟

**المميزات:**

1. **استخدام نفس التقنية**
   - نحن نستخدم React 19 في الموقع
   - React Native هو نفس المفاهيم
   - سهولة نقل المطورين بين المشروعين
   - إعادة استخدام الكود (70%+)

2. **Expo SDK**
   - تطوير أسرع بـ 3 مرات
   - أدوات جاهزة للكاميرا، الإشعارات، التخزين
   - Hot Reload سريع جداً
   - بناء سحابي (EAS Build)
   - OTA Updates (تحديثات فورية بدون متجر)

3. **مجتمع ضخم**
   - مكتبات عربية جاهزة
   - حلول جاهزة للمشاكل
   - توثيق ممتاز بالعربية

4. **تكلفة أقل**
   - تطبيق واحد لـ iOS + Android
   - مطور واحد بدل اثنين
   - وقت أقل = تكلفة أقل

### 📦 Stack التقني الكامل

#### Frontend (Mobile App)

```javascript
{
  "core": {
    "react-native": "0.73.x",
    "expo": "~50.x",
    "typescript": "5.x"
  },
  "navigation": {
    "react-navigation": "^6.x",  // التنقل
    "@react-navigation/native-stack": "^6.x",
    "@react-navigation/bottom-tabs": "^6.x",
    "@react-navigation/drawer": "^6.x"
  },
  "state": {
    "zustand": "^4.x",           // إدارة الحالة (خفيف)
    "@tanstack/react-query": "^5.x"  // Cache + Sync
  },
  "ui": {
    "react-native-paper": "^5.x",    // Material Design
    "react-native-elements": "^4.x",
    "react-native-vector-icons": "^10.x",
    "lottie-react-native": "^6.x"    // أنيميشن
  },
  "storage": {
    "@react-native-async-storage/async-storage": "^1.x",
    "expo-secure-store": "~12.x",    // تخزين آمن
    "expo-file-system": "~16.x"      // نظام الملفات
  },
  "offline": {
    "@react-native-community/netinfo": "^11.x",
    "watermelondb": "^0.27.x"        // قاعدة بيانات محلية
  },
  "auth": {
    "expo-auth-session": "~5.x",
    "expo-crypto": "~12.x"
  },
  "notifications": {
    "expo-notifications": "~0.27.x"
  },
  "utilities": {
    "react-native-reanimated": "~3.x",  // أنيميشن متقدم
    "react-native-gesture-handler": "~2.x",
    "axios": "^1.x",
    "date-fns": "^3.x",
    "i18next": "^23.x"               // ترجمة
  }
}
```

#### Backend (Server)

```javascript
{
  "current": {
    "nextjs": "16.1.1",
    "prisma": "7.2.0",
    "postgresql": "latest"
  },
  "additions": {
    "jsonwebtoken": "^9.x",          // JWT
    "bcryptjs": "^2.x",              // Hashing
    "expo-server-sdk": "^3.x",       // Push Notifications
    "multer": "^1.x",                // File Upload
    "express-rate-limit": "^7.x"     // Rate Limiting
  }
}
```

### 🏗️ البنية المعمارية

```
┌─────────────────────────────────────────────┐
│           📱 React Native App               │
│  ┌─────────────┬──────────────┬──────────┐ │
│  │   iOS App   │  Android App │   Web    │ │
│  └─────────────┴──────────────┴──────────┘ │
└──────────────────┬──────────────────────────┘
                   │
         ┌─────────▼─────────┐
         │   REST API (JWT)  │
         │   Next.js Routes  │
         └─────────┬─────────┘
                   │
         ┌─────────▼─────────┐
         │   Prisma ORM      │
         └─────────┬─────────┘
                   │
         ┌─────────▼─────────┐
         │   PostgreSQL      │
         │  (Shared Database)│
         └───────────────────┘
```

---

## 4. هيكلة قاعدة البيانات المشتركة

### 📊 المخطط الموسع (Extended Schema)

#### A. الجداول الجديدة

##### 1. جدول الأجهزة (Devices)

```prisma
model Device {
  id            String   @id @default(cuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])

  deviceToken   String   @unique  // للإشعارات
  deviceType    String              // "ios" | "android"
  deviceModel   String?             // "iPhone 14 Pro"
  osVersion     String?             // "iOS 17.0"
  appVersion    String              // "1.0.0"

  isActive      Boolean  @default(true)
  lastSeen      DateTime @default(now())

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([userId])
  @@index([deviceToken])
}
```

**الاستخدام:**
- تسجيل الأجهزة لكل مستخدم
- إرسال الإشعارات المستهدفة
- تتبع النسخ النشطة
- إدارة الجلسات المتعددة

##### 2. جدول تتبع القراءة (ReadingProgress)

```prisma
model ReadingProgress {
  id            String   @id @default(cuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])
  bookId        String
  book          Book     @relation(fields: [bookId], references: [id])

  currentPage   Int      @default(1)
  totalPages    Int
  percentage    Float    @default(0)

  lastPosition  Json?    // { chapterId, sectionId, scrollPosition }

  startedAt     DateTime @default(now())
  lastReadAt    DateTime @default(now())
  completedAt   DateTime?

  readingTime   Int      @default(0)  // بالدقائق

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@unique([userId, bookId])
  @@index([userId])
  @@index([bookId])
}
```

**الاستخدام:**
- تتبع موقع القراءة الدقيق
- حساب نسبة الإنجاز
- إحصائيات وقت القراءة
- العودة لآخر صفحة

##### 3. جدول الإشعارات (Notifications)

```prisma
model Notification {
  id            String   @id @default(cuid())
  userId        String?
  user          User?    @relation(fields: [userId], references: [id])

  title         String
  body          String
  data          Json?    // بيانات إضافية

  type          String   // "new_book", "update", "reminder"
  priority      String   @default("normal")  // "high", "normal", "low"

  isRead        Boolean  @default(false)
  isSent        Boolean  @default(false)
  sentAt        DateTime?

  scheduledFor  DateTime?

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([userId])
  @@index([isRead])
  @@index([scheduledFor])
}
```

**الاستخدام:**
- إشعارات الكتب الجديدة
- تذكيرات القراءة اليومية
- تحديثات المحتوى
- رسائل إدارية

##### 4. جدول التنزيلات (OfflineDownloads)

```prisma
model OfflineDownload {
  id            String   @id @default(cuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])
  bookId        String
  book          Book     @relation(fields: [bookId], references: [id])

  status        String   @default("pending")  // pending, downloading, completed, failed
  progress      Float    @default(0)

  fileSize      BigInt?
  downloadedSize BigInt  @default(0)

  startedAt     DateTime?
  completedAt   DateTime?
  expiresAt     DateTime?  // تلقائي بعد 30 يوم

  error         String?

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@unique([userId, bookId])
  @@index([userId])
  @@index([status])
}
```

**الاستخدام:**
- تنزيل الكتب للقراءة الأوفلاين
- تتبع حالة التنزيل
- إدارة المساحة
- انتهاء الصلاحية التلقائي

##### 5. جدول المزامنة (SyncQueue)

```prisma
model SyncQueue {
  id            String   @id @default(cuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])
  deviceId      String

  action        String   // "create", "update", "delete"
  entityType    String   // "bookmark", "highlight", "progress"
  entityId      String
  data          Json

  status        String   @default("pending")
  retryCount    Int      @default(0)
  lastError     String?

  createdAt     DateTime @default(now())
  processedAt   DateTime?

  @@index([userId])
  @@index([deviceId])
  @@index([status])
}
```

**الاستخدام:**
- مزامنة البيانات عند الأوفلاين
- معالجة التعارضات
- ضمان عدم فقدان البيانات

##### 6. جدول الجلسات (Sessions)

```prisma
model Session {
  id            String   @id @default(cuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])
  deviceId      String?

  refreshToken  String   @unique
  accessToken   String

  ipAddress     String?
  userAgent     String?

  isValid       Boolean  @default(true)
  expiresAt     DateTime

  createdAt     DateTime @default(now())
  lastUsedAt    DateTime @default(now())

  @@index([userId])
  @@index([refreshToken])
  @@index([isValid])
}
```

**الاستخدام:**
- إدارة الجلسات المتعددة
- Refresh Tokens
- إلغاء الجلسات عن بعد
- الأمان

#### B. تعديلات الجداول الحالية

##### 1. جدول المستخدمين (Users) - إضافات

```prisma
model User {
  // ... الحقول الموجودة ...

  // إضافات جديدة للموبايل
  phoneNumber       String?    @unique
  phoneVerified     Boolean    @default(false)

  profilePicture    String?
  bio               String?

  preferences       Json?      // إعدادات المستخدم
  settings          Json?      // إعدادات التطبيق

  lastLoginAt       DateTime?
  lastLoginDevice   String?

  isPremium         Boolean    @default(false)
  premiumExpiresAt  DateTime?

  // العلاقات الجديدة
  devices           Device[]
  readingProgress   ReadingProgress[]
  notifications     Notification[]
  offlineDownloads  OfflineDownload[]
  syncQueue         SyncQueue[]
  sessions          Session[]
}
```

##### 2. جدول الكتب (Books) - إضافات

```prisma
model Book {
  // ... الحقول الموجودة ...

  // إضافات للموبايل
  audioUrl          String?    // رابط الصوتي
  audioSize         BigInt?

  pdfUrl            String?    // رابط PDF
  pdfSize           BigInt?

  epubUrl           String?    // رابط EPUB
  epubSize          BigInt?

  allowOffline      Boolean    @default(true)
  offlineSize       BigInt?    // حجم النسخة الأوفلاين

  downloadCount     Int        @default(0)
  rating            Float?
  ratingCount       Int        @default(0)

  // العلاقات الجديدة
  readingProgress   ReadingProgress[]
  offlineDownloads  OfflineDownload[]
}
```

### 🔄 Migration Plan

**الخطوات:**

```bash
# 1. إنشاء ملف migration
npx prisma migrate dev --name add_mobile_support

# 2. تطبيق على قاعدة البيانات
npx prisma migrate deploy

# 3. إعادة توليد Prisma Client
npx prisma generate

# 4. Seed البيانات الجديدة
npx prisma db seed
```

---

## 5. تصميم الـ API

### 🔐 Authentication System

#### A. نظام المصادقة بـ JWT

**Token Structure:**

```javascript
// Access Token (قصير المدى - 15 دقيقة)
{
  userId: "user_id",
  email: "user@email.com",
  role: "user",
  deviceId: "device_id",
  type: "access",
  iat: 1234567890,
  exp: 1234568790
}

// Refresh Token (طويل المدى - 30 يوم)
{
  userId: "user_id",
  deviceId: "device_id",
  type: "refresh",
  iat: 1234567890,
  exp: 1237159890
}
```

#### B. API Endpoints للمصادقة

##### 1. التسجيل (Register)

```
POST /api/v1/auth/register
Content-Type: application/json

Request:
{
  "email": "user@example.com",
  "password": "SecurePass123!",
  "name": "أحمد محمد",
  "phoneNumber": "+966501234567",
  "deviceInfo": {
    "deviceToken": "ExponentPushToken[xxx]",
    "deviceType": "ios",
    "deviceModel": "iPhone 14 Pro",
    "osVersion": "17.0",
    "appVersion": "1.0.0"
  }
}

Response: 200
{
  "success": true,
  "data": {
    "user": {
      "id": "user_id",
      "email": "user@example.com",
      "name": "أحمد محمد"
    },
    "tokens": {
      "accessToken": "eyJhbGc...",
      "refreshToken": "eyJhbGc...",
      "expiresIn": 900
    }
  }
}
```

##### 2. تسجيل الدخول (Login)

```
POST /api/v1/auth/login

Request:
{
  "email": "user@example.com",
  "password": "SecurePass123!",
  "deviceInfo": { ... }
}

Response: 200
{
  "success": true,
  "data": {
    "user": { ... },
    "tokens": { ... }
  }
}
```

##### 3. تحديث Token

```
POST /api/v1/auth/refresh

Headers:
Authorization: Bearer <refresh_token>

Response: 200
{
  "success": true,
  "data": {
    "accessToken": "eyJhbGc...",
    "expiresIn": 900
  }
}
```

##### 4. تسجيل الخروج

```
POST /api/v1/auth/logout

Headers:
Authorization: Bearer <access_token>

Response: 200
{
  "success": true,
  "message": "تم تسجيل الخروج بنجاح"
}
```

### 📚 API Endpoints للكتب

##### 1. قائمة الكتب

```
GET /api/v1/books?page=1&limit=20&category=fiqh&search=صلاة

Response: 200
{
  "success": true,
  "data": {
    "books": [
      {
        "id": "book_id",
        "title": "كتاب الصلاة",
        "author": "المؤلف",
        "coverImage": "https://...",
        "category": {
          "id": "cat_id",
          "arabicName": "الفقه"
        },
        "pageCount": 450,
        "readingProgress": {
          "percentage": 45.5,
          "currentPage": 205
        },
        "isDownloaded": false,
        "offlineSize": 25600000
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 20,
      "total": 150,
      "totalPages": 8
    }
  }
}
```

##### 2. تفاصيل الكتاب

```
GET /api/v1/books/:bookId

Response: 200
{
  "success": true,
  "data": {
    "book": {
      "id": "book_id",
      "title": "كتاب الصلاة",
      "author": "المؤلف",
      "description": "وصف الكتاب...",
      "coverImage": "https://...",
      "pageCount": 450,
      "category": { ... },
      "chapters": [
        {
          "id": "chapter_1",
          "title": "الفصل الأول",
          "order": 1,
          "sections": [
            {
              "id": "section_1",
              "title": "القسم الأول",
              "content": "المحتوى...",
              "pageCount": 15
            }
          ]
        }
      ],
      "readingProgress": { ... },
      "bookmarks": [ ... ],
      "highlights": [ ... ],
      "downloadInfo": {
        "isDownloaded": false,
        "canDownload": true,
        "size": 25600000,
        "formats": ["pdf", "epub"]
      }
    }
  }
}
```

##### 3. محتوى الكتاب (للقراءة)

```
GET /api/v1/books/:bookId/content?chapter=1&section=1

Response: 200
{
  "success": true,
  "data": {
    "content": {
      "chapterId": "chapter_1",
      "sectionId": "section_1",
      "title": "القسم الأول",
      "content": "<p>المحتوى المنسق...</p>",
      "pageNumber": 15,
      "navigation": {
        "prev": {
          "chapterId": "...",
          "sectionId": "...",
          "title": "..."
        },
        "next": { ... }
      }
    }
  }
}
```

### 🔖 API للعلامات المرجعية

##### 1. إضافة علامة مرجعية

```
POST /api/v1/bookmarks

Request:
{
  "bookId": "book_id",
  "chapterId": "chapter_id",
  "sectionId": "section_id",
  "pageNumber": 125,
  "note": "ملاحظة مهمة"
}

Response: 201
{
  "success": true,
  "data": {
    "bookmark": {
      "id": "bookmark_id",
      "bookId": "book_id",
      "note": "ملاحظة مهمة",
      "createdAt": "2025-01-09T..."
    }
  }
}
```

##### 2. قائمة العلامات المرجعية

```
GET /api/v1/bookmarks?bookId=book_id

Response: 200
{
  "success": true,
  "data": {
    "bookmarks": [
      {
        "id": "bookmark_id",
        "book": {
          "id": "book_id",
          "title": "اسم الكتاب",
          "coverImage": "..."
        },
        "pageNumber": 125,
        "note": "ملاحظة",
        "createdAt": "..."
      }
    ]
  }
}
```

### 📊 API لتتبع القراءة

##### 1. تحديث تقدم القراءة

```
PUT /api/v1/reading-progress/:bookId

Request:
{
  "currentPage": 205,
  "percentage": 45.5,
  "lastPosition": {
    "chapterId": "chapter_5",
    "sectionId": "section_2",
    "scrollPosition": 1500
  },
  "readingTime": 45
}

Response: 200
{
  "success": true,
  "data": {
    "progress": { ... }
  }
}
```

##### 2. إحصائيات القراءة

```
GET /api/v1/reading-progress/stats

Response: 200
{
  "success": true,
  "data": {
    "stats": {
      "totalBooksRead": 15,
      "totalPagesRead": 3420,
      "totalReadingTime": 1850,
      "currentStreak": 12,
      "longestStreak": 45,
      "favoriteCategory": "الفقه",
      "recentBooks": [ ... ],
      "weeklyProgress": [
        { "day": "السبت", "pages": 45 },
        { "day": "الأحد", "pages": 67 }
      ]
    }
  }
}
```

### 📥 API للتنزيلات

##### 1. تنزيل كتاب

```
POST /api/v1/downloads

Request:
{
  "bookId": "book_id",
  "format": "pdf"
}

Response: 200
{
  "success": true,
  "data": {
    "download": {
      "id": "download_id",
      "bookId": "book_id",
      "status": "pending",
      "downloadUrl": "https://...",
      "fileSize": 25600000,
      "expiresAt": "2025-02-09T..."
    }
  }
}
```

##### 2. حالة التنزيل

```
GET /api/v1/downloads/:downloadId

Response: 200
{
  "success": true,
  "data": {
    "download": {
      "id": "download_id",
      "status": "completed",
      "progress": 100,
      "downloadedSize": 25600000,
      "completedAt": "..."
    }
  }
}
```

### 🔔 API للإشعارات

##### 1. تسجيل Device Token

```
POST /api/v1/notifications/register

Request:
{
  "deviceToken": "ExponentPushToken[xxx]",
  "deviceType": "ios"
}

Response: 200
{
  "success": true
}
```

##### 2. قائمة الإشعارات

```
GET /api/v1/notifications?page=1&limit=20

Response: 200
{
  "success": true,
  "data": {
    "notifications": [
      {
        "id": "notif_id",
        "title": "كتاب جديد",
        "body": "تم إضافة كتاب جديد في الفقه",
        "type": "new_book",
        "data": {
          "bookId": "book_id"
        },
        "isRead": false,
        "createdAt": "..."
      }
    ],
    "unreadCount": 5
  }
}
```

### 🔄 API للمزامنة

##### 1. مزامنة البيانات

```
POST /api/v1/sync

Request:
{
  "lastSyncAt": "2025-01-09T10:00:00Z",
  "pendingActions": [
    {
      "action": "create",
      "entityType": "bookmark",
      "data": { ... }
    },
    {
      "action": "update",
      "entityType": "progress",
      "entityId": "...",
      "data": { ... }
    }
  ]
}

Response: 200
{
  "success": true,
  "data": {
    "processed": 2,
    "conflicts": [],
    "updates": {
      "bookmarks": [ ... ],
      "progress": [ ... ],
      "highlights": [ ... ]
    },
    "syncedAt": "2025-01-09T11:00:00Z"
  }
}
```

### 📋 API Response Format (معياري)

**Success Response:**

```javascript
{
  "success": true,
  "data": {
    // البيانات المطلوبة
  },
  "meta": {
    // معلومات إضافية (pagination, etc)
  }
}
```

**Error Response:**

```javascript
{
  "success": false,
  "error": {
    "code": "INVALID_INPUT",
    "message": "البريد الإلكتروني غير صحيح",
    "details": {
      "field": "email",
      "value": "invalid-email"
    }
  }
}
```

**HTTP Status Codes:**

- `200` - نجح الطلب
- `201` - تم الإنشاء بنجاح
- `400` - خطأ في البيانات المرسلة
- `401` - غير مصرح (Unauthorized)
- `403` - ممنوع (Forbidden)
- `404` - غير موجود
- `429` - تجاوز الحد المسموح
- `500` - خطأ في السيرفر

---

## 6. هيكلة تطبيق الموبايل

### 📂 هيكل المشروع

```
islamic-library-mobile/
├── 📂 src/
│   ├── 📂 api/                     # API Calls
│   │   ├── client.ts              # Axios Config
│   │   ├── auth.api.ts
│   │   ├── books.api.ts
│   │   ├── bookmarks.api.ts
│   │   └── notifications.api.ts
│   │
│   ├── 📂 screens/                # الشاشات
│   │   ├── 📂 auth/
│   │   │   ├── LoginScreen.tsx
│   │   │   ├── RegisterScreen.tsx
│   │   │   └── ForgotPasswordScreen.tsx
│   │   │
│   │   ├── 📂 home/
│   │   │   ├── HomeScreen.tsx
│   │   │   ├── SearchScreen.tsx
│   │   │   └── CategoriesScreen.tsx
│   │   │
│   │   ├── 📂 books/
│   │   │   ├── BookListScreen.tsx
│   │   │   ├── BookDetailScreen.tsx
│   │   │   └── ReaderScreen.tsx
│   │   │
│   │   ├── 📂 library/
│   │   │   ├── LibraryScreen.tsx
│   │   │   ├── BookmarksScreen.tsx
│   │   │   ├── HighlightsScreen.tsx
│   │   │   └── DownloadsScreen.tsx
│   │   │
│   │   ├── 📂 profile/
│   │   │   ├── ProfileScreen.tsx
│   │   │   ├── SettingsScreen.tsx
│   │   │   └── StatisticsScreen.tsx
│   │   │
│   │   └── 📂 onboarding/
│   │       ├── WelcomeScreen.tsx
│   │       └── TutorialScreen.tsx
│   │
│   ├── 📂 components/             # المكونات المشتركة
│   │   ├── 📂 ui/
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Loading.tsx
│   │   │   └── EmptyState.tsx
│   │   │
│   │   ├── 📂 books/
│   │   │   ├── BookCard.tsx
│   │   │   ├── BookList.tsx
│   │   │   └── BookCover.tsx
│   │   │
│   │   ├── 📂 reader/
│   │   │   ├── ReaderControls.tsx
│   │   │   ├── BookmarkButton.tsx
│   │   │   └── HighlightMenu.tsx
│   │   │
│   │   └── 📂 layout/
│   │       ├── Header.tsx
│   │       ├── TabBar.tsx
│   │       └── Drawer.tsx
│   │
│   ├── 📂 navigation/             # التنقل
│   │   ├── RootNavigator.tsx
│   │   ├── AuthNavigator.tsx
│   │   ├── MainNavigator.tsx
│   │   └── types.ts
│   │
│   ├── 📂 stores/                 # State Management (Zustand)
│   │   ├── authStore.ts
│   │   ├── booksStore.ts
│   │   ├── readerStore.ts
│   │   └── offlineStore.ts
│   │
│   ├── 📂 hooks/                  # Custom Hooks
│   │   ├── useAuth.ts
│   │   ├── useBooks.ts
│   │   ├── useOffline.ts
│   │   └── useNotifications.ts
│   │
│   ├── 📂 services/               # Business Logic
│   │   ├── authService.ts
│   │   ├── syncService.ts
│   │   ├── downloadService.ts
│   │   └── notificationService.ts
│   │
│   ├── 📂 utils/                  # Utilities
│   │   ├── storage.ts
│   │   ├── dateUtils.ts
│   │   ├── validators.ts
│   │   └── helpers.ts
│   │
│   ├── 📂 constants/              # الثوابت
│   │   ├── colors.ts
│   │   ├── sizes.ts
│   │   ├── api.ts
│   │   └── config.ts
│   │
│   ├── 📂 types/                  # TypeScript Types
│   │   ├── api.types.ts
│   │   ├── models.types.ts
│   │   └── navigation.types.ts
│   │
│   ├── 📂 assets/                 # الأصول
│   │   ├── 📂 images/
│   │   ├── 📂 icons/
│   │   ├── 📂 fonts/
│   │   └── 📂 animations/
│   │
│   └── 📂 i18n/                   # الترجمة
│       ├── ar.json
│       └── en.json
│
├── 📄 App.tsx                     # نقطة البداية
├── 📄 app.json                    # Expo Config
├── 📄 package.json
├── 📄 tsconfig.json
└── 📄 .env

```

### 🎨 مثال على الكود

#### 1. API Client

```typescript
// src/api/client.ts
import axios from 'axios';
import * as SecureStore from 'expo-secure-store';
import { API_URL } from '@/constants/api';

const apiClient = axios.create({
  baseURL: API_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Request Interceptor
apiClient.interceptors.request.use(
  async (config) => {
    const token = await SecureStore.getItemAsync('accessToken');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// Response Interceptor
apiClient.interceptors.response.use(
  (response) => response.data,
  async (error) => {
    const originalRequest = error.config;

    // إذا انتهت صلاحية Token
    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;

      try {
        const refreshToken = await SecureStore.getItemAsync('refreshToken');
        const { data } = await axios.post(`${API_URL}/auth/refresh`, {
          refreshToken,
        });

        await SecureStore.setItemAsync('accessToken', data.accessToken);
        originalRequest.headers.Authorization = `Bearer ${data.accessToken}`;

        return apiClient(originalRequest);
      } catch {
        // إعادة توجيه للـ Login
        return Promise.reject(error);
      }
    }

    return Promise.reject(error);
  }
);

export default apiClient;
```

#### 2. Auth Store (Zustand)

```typescript
// src/stores/authStore.ts
import { create } from 'zustand';
import * as SecureStore from 'expo-secure-store';
import { authAPI } from '@/api/auth.api';

interface User {
  id: string;
  email: string;
  name: string;
  profilePicture?: string;
}

interface AuthStore {
  user: User | null;
  isAuthenticated: boolean;
  isLoading: boolean;

  login: (email: string, password: string) => Promise<void>;
  register: (data: RegisterData) => Promise<void>;
  logout: () => Promise<void>;
  loadUser: () => Promise<void>;
}

export const useAuthStore = create<AuthStore>((set) => ({
  user: null,
  isAuthenticated: false,
  isLoading: true,

  login: async (email, password) => {
    try {
      const response = await authAPI.login(email, password);

      await SecureStore.setItemAsync('accessToken', response.tokens.accessToken);
      await SecureStore.setItemAsync('refreshToken', response.tokens.refreshToken);

      set({
        user: response.user,
        isAuthenticated: true,
      });
    } catch (error) {
      throw error;
    }
  },

  register: async (data) => {
    try {
      const response = await authAPI.register(data);

      await SecureStore.setItemAsync('accessToken', response.tokens.accessToken);
      await SecureStore.setItemAsync('refreshToken', response.tokens.refreshToken);

      set({
        user: response.user,
        isAuthenticated: true,
      });
    } catch (error) {
      throw error;
    }
  },

  logout: async () => {
    try {
      await authAPI.logout();
      await SecureStore.deleteItemAsync('accessToken');
      await SecureStore.deleteItemAsync('refreshToken');

      set({
        user: null,
        isAuthenticated: false,
      });
    } catch (error) {
      throw error;
    }
  },

  loadUser: async () => {
    try {
      const token = await SecureStore.getItemAsync('accessToken');

      if (token) {
        const user = await authAPI.getProfile();
        set({
          user,
          isAuthenticated: true,
          isLoading: false,
        });
      } else {
        set({ isLoading: false });
      }
    } catch (error) {
      set({ isLoading: false });
    }
  },
}));
```

#### 3. مثال شاشة (HomeScreen)

```typescript
// src/screens/home/HomeScreen.tsx
import React from 'react';
import { View, ScrollView, StyleSheet } from 'react-native';
import { useQuery } from '@tanstack/react-query';
import { booksAPI } from '@/api/books.api';
import BookCard from '@/components/books/BookCard';
import Loading from '@/components/ui/Loading';

export default function HomeScreen() {
  const { data, isLoading } = useQuery({
    queryKey: ['books', 'recent'],
    queryFn: () => booksAPI.getRecentBooks(),
  });

  if (isLoading) {
    return <Loading />;
  }

  return (
    <ScrollView style={styles.container}>
      <View style={styles.section}>
        <Text style={styles.sectionTitle}>الكتب الحديثة</Text>
        {data?.books.map((book) => (
          <BookCard key={book.id} book={book} />
        ))}
      </View>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  section: {
    padding: 16,
  },
  sectionTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 16,
    textAlign: 'right',
  },
});
```

---

_سأكمل الباقي في الرسالة القادمة..._
## 11. الإشعارات (Push Notifications)

### 🔔 نظام الإشعارات

#### 1. Expo Notifications Setup

**التكوين:**
```typescript
import * as Notifications from 'expo-notifications';
import * as Device from 'expo-device';
import Constants from 'expo-constants';

// إعداد handler للإشعارات
Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowAlert: true,
    shouldPlaySound: true,
    shouldSetBadge: true,
  }),
});

// طلب أذونات
const registerForPushNotificationsAsync = async () => {
  if (Device.isDevice) {
    const { status: existingStatus } =
      await Notifications.getPermissionsAsync();

    let finalStatus = existingStatus;

    if (existingStatus !== 'granted') {
      const { status } = await Notifications
        .requestPermissionsAsync();
      finalStatus = status;
    }

    if (finalStatus !== 'granted') {
      alert('فشل في الحصول على إذن الإشعارات!');
      return;
    }

    const token = await Notifications
      .getExpoPushTokenAsync({
        projectId: Constants.expoConfig.extra.eas.projectId,
      });

    // إرسال Token للسيرفر
    await registerDeviceToken(token.data);

    return token.data;
  }
};
```

#### 2. أنواع الإشعارات

**كتاب جديد:**
```typescript
{
  title: "كتاب جديد في الفقه 📚",
  body: "تمت إضافة كتاب 'أحكام الصلاة' - ابدأ القراءة الآن!",
  data: {
    type: 'new_book',
    bookId: 'book_123',
    categoryId: 'fiqh'
  }
}
```

**تذكير القراءة:**
```typescript
{
  title: "وقت القراءة! 📖",
  body: "لم تقرأ اليوم بعد. استكمل قراءة 'كتاب الصلاة'",
  data: {
    type: 'reading_reminder',
    bookId: 'book_123'
  }
}
```

**إنجاز جديد:**
```typescript
{
  title: "إنجاز جديد! 🎉",
  body: "مبروك! أكملت قراءة 10 كتب",
  data: {
    type: 'achievement',
    achievementId: 'books_10'
  }
}
```

#### 3. جدولة الإشعارات المحلية

**تذكير يومي:**
```typescript
const scheduleDailyReminder = async (hour: number, minute: number) => {
  await Notifications.cancelAllScheduledNotificationsAsync();

  await Notifications.scheduleNotificationAsync({
    content: {
      title: 'وقت القراءة! 📖',
      body: 'خصص بعض الوقت للقراءة اليوم',
      data: { type: 'daily_reminder' },
    },
    trigger: {
      hour,
      minute,
      repeats: true,
    },
  });
};
```

**تذكير باستكمال القراءة:**
```typescript
const scheduleBookReminderenderer = async (bookId: string, days: number) => {
  await Notifications.scheduleNotificationAsync({
    content: {
      title: 'استكمل القراءة',
      body: 'لديك كتاب لم تكمله منذ أسبوع',
      data: { type: 'book_reminder', bookId },
    },
    trigger: {
      seconds: days * 24 * 60 * 60,
    },
  });
};
```

#### 4. معالجة الإشعارات

**عند الضغط على إشعار:**
```typescript
const notificationResponseListener = useRef();

useEffect(() => {
  notificationResponseListener.current = Notifications
    .addNotificationResponseReceivedListener(response => {
      const data = response.notification.request.content.data;

      switch (data.type) {
        case 'new_book':
          navigation.navigate('BookDetail', {
            bookId: data.bookId
          });
          break;

        case 'reading_reminder':
          navigation.navigate('Reader', {
            bookId: data.bookId
          });
          break;

        case 'achievement':
          navigation.navigate('Achievements');
          break;
      }
    });

  return () => {
    Notifications.removeNotificationSubscription(
      notificationResponseListener.current
    );
  };
}, []);
```

#### 5. Badge Count

**تحديث العداد:**
```typescript
const updateBadgeCount = async (count: number) => {
  await Notifications.setBadgeCountAsync(count);
};

// عند فتح التطبيق
useEffect(() => {
  const subscription = AppState.addEventListener(
    'change',
    nextAppState => {
      if (nextAppState === 'active') {
        updateBadgeCount(0); // مسح العداد
      }
    }
  );

  return () => subscription.remove();
}, []);
```

---

## 12. الأداء والتحسينات

### ⚡ استراتيجيات الأداء

#### 1. تحسين القوائم (FlatList Optimization)

**استخدام مفاتيح فريدة:**
```typescript
<FlatList
  data={books}
  keyExtractor={(item) => item.id}
  renderItem={({ item }) => <BookCard book={item} />}

  // تحسينات الأداء
  initialNumToRender={10}
  maxToRenderPerBatch={10}
  updateCellsBatchingPeriod={50}
  windowSize={21}
  removeClippedSubviews={true}

  // Memo للعنصر
  getItemLayout={(data, index) => ({
    length: 200,
    offset: 200 * index,
    index,
  })}
/>
```

**Memoization:**
```typescript
import React, { memo } from 'react';

const BookCard = memo(({ book }) => {
  return (
    <Card>
      <FastImage source={{ uri: book.coverImage }} />
      <Text>{book.title}</Text>
    </Card>
  );
}, (prevProps, nextProps) => {
  return prevProps.book.id === nextProps.book.id;
});
```

#### 2. Lazy Loading & Code Splitting

**React.lazy للشاشات:**
```typescript
const ReaderScreen = lazy(() => import('./screens/ReaderScreen'));
const BookDetailScreen = lazy(() => import('./screens/BookDetailScreen'));

// في Navigator
<Stack.Screen
  name="Reader"
  component={ReaderScreen}
  options={{ lazy: true }}
/>
```

**Dynamic Import:**
```typescript
// تحميل المكتبات الكبيرة عند الحاجة
const loadPDFLibrary = async () => {
  const PDF = await import('react-native-pdf');
  return PDF.default;
};
```

#### 3. تحسين الصور

**Image Optimization:**
```typescript
// استخدام Fast Image
import FastImage from 'react-native-fast-image';

<FastImage
  source={{
    uri: imageUrl,
    priority: FastImage.priority.normal,
    cache: FastImage.cacheControl.immutable
  }}
  resizeMode={FastImage.resizeMode.cover}
  style={styles.image}
/>

// Placeholder أثناء التحميل
<FastImage
  source={{ uri: imageUrl }}
  defaultSource={require('./placeholder.png')}
/>
```

**تحميل تدريجي:**
```typescript
import { Image } from 'expo-image';

<Image
  source={imageUrl}
  placeholder={blurhash}
  contentFit="cover"
  transition={200}
/>
```

#### 4. تحسين الأنيميشن

**استخدام Reanimated:**
```typescript
import Animated, {
  useSharedValue,
  useAnimatedStyle,
  withSpring
} from 'react-native-reanimated';

const AnimatedButton = () => {
  const scale = useSharedValue(1);

  const animatedStyle = useAnimatedStyle(() => ({
    transform: [{ scale: scale.value }]
  }));

  const onPress = () => {
    scale.value = withSpring(0.95, {}, () => {
      scale.value = withSpring(1);
    });
  };

  return (
    <Animated.View style={animatedStyle}>
      <Pressable onPress={onPress}>
        <Text>اضغط هنا</Text>
      </Pressable>
    </Animated.View>
  );
};
```

#### 5. تقليل Re-renders

**useMemo & useCallback:**
```typescript
const BookList = ({ books, onBookPress }) => {
  // حفظ القائمة المفلترة
  const filteredBooks = useMemo(() => {
    return books.filter(book => book.isPublished);
  }, [books]);

  // حفظ دالة الضغط
  const handlePress = useCallback((bookId) => {
    onBookPress(bookId);
  }, [onBookPress]);

  return (
    <FlatList
      data={filteredBooks}
      renderItem={({ item }) => (
        <BookCard
          book={item}
          onPress={handlePress}
        />
      )}
    />
  );
};
```

#### 6. Memory Management

**تنظيف Memory Leaks:**
```typescript
useEffect(() => {
  const subscription = someObservable.subscribe();

  return () => {
    subscription.unsubscribe(); // تنظيف
  };
}, []);
```

**إلغاء Async Operations:**
```typescript
useEffect(() => {
  let isMounted = true;

  const fetchData = async () => {
    const data = await API.getData();
    if (isMounted) {
      setData(data);
    }
  };

  fetchData();

  return () => {
    isMounted = false;
  };
}, []);
```

#### 7. Bundle Size Optimization

**تقليل حجم Bundle:**
```javascript
// metro.config.js
module.exports = {
  transformer: {
    minifierConfig: {
      compress: {
        drop_console: true, // حذف console.log
      }
    }
  }
};
```

**Tree Shaking:**
```typescript
// استيراد ما تحتاجه فقط
import { Button } from 'react-native-paper';
// بدلاً من
import * as Paper from 'react-native-paper';
```

#### 8. Network Optimization

**Request Batching:**
```typescript
// جمع عدة طلبات في طلب واحد
const batchRequests = async (requests) => {
  return await API.batch({
    requests: requests.map(r => ({
      method: r.method,
      url: r.url,
      data: r.data
    }))
  });
};
```

**Request Debouncing:**
```typescript
import { debounce } from 'lodash';

const searchBooks = debounce(async (query) => {
  const results = await API.search(query);
  setResults(results);
}, 300); // انتظار 300ms قبل البحث
```

---

## 13. خطة التنفيذ المرحلية

### 📅 المرحلة 1: الأساسيات (4-6 أسابيع)

#### الأسبوع 1-2: إعداد المشروع والبنية التحتية

**المهام:**
- ✅ إنشاء مشروع Expo جديد
- ✅ إعداد TypeScript
- ✅ تكوين Navigation
- ✅ إعداد State Management (Zustand)
- ✅ إعداد API Client (Axios)
- ✅ إعداد React Query
- ✅ تكوين Theme System
- ✅ إعداد البيئات (.env)

**Backend:**
- ✅ إضافة JWT إلى API
- ✅ إنشاء Endpoints للموبايل
- ✅ Migration لقاعدة البيانات
- ✅ Seed البيانات

#### الأسبوع 3-4: المصادقة والملف الشخصي

**Features:**
- ✅ شاشة Splash & Onboarding
- ✅ تسجيل الدخول
- ✅ التسجيل
- ✅ استعادة كلمة المرور
- ✅ الملف الشخصي
- ✅ الإعدادات الأساسية

**Testing:**
- ✅ Unit Tests
- ✅ Integration Tests
- ✅ اختبار يدوي على الأجهزة

#### الأسبوع 5-6: تصفح الكتب

**Features:**
- ✅ الصفحة الرئيسية
- ✅ قائمة الكتب
- ✅ تفاصيل الكتاب
- ✅ البحث
- ✅ الفئات
- ✅ التصفية والترتيب

**UI/UX:**
- ✅ تصميم الشاشات
- ✅ الأنيميشن
- ✅ Loading States
- ✅ Error Handling

---

### 📅 المرحلة 2: القراءة والمكتبة (4-5 أسابيع)

#### الأسبوع 7-9: القارئ

**Features:**
- ✅ واجهة القراءة الأساسية
- ✅ التنقل بين الصفحات
- ✅ قائمة الفصول
- ✅ شريط التقدم
- ✅ إعدادات القارئ (خط، حجم، ألوان)
- ✅ الوضع الليلي

**Advanced:**
- ✅ العلامات المرجعية
- ✅ التظليل
- ✅ الملاحظات
- ✅ البحث في الكتاب
- ✅ مشاركة الاقتباسات

#### الأسبوع 10-11: المكتبة الشخصية

**Features:**
- ✅ كتبي
- ✅ الحالي / المنتهية
- ✅ قائمة القراءة
- ✅ العلامات المرجعية
- ✅ التظليلات
- ✅ الإحصائيات الأساسية

---

### 📅 المرحلة 3: الأوفلاين والمزامنة (3-4 أسابيع)

#### الأسبوع 12-13: التنزيلات

**Features:**
- ✅ تنزيل الكتب
- ✅ تتبع التقدم
- ✅ إدارة التنزيلات
- ✅ التحميل التلقائي (WiFi)
- ✅ إدارة المساحة

**Backend:**
- ✅ API للتنزيل
- ✅ ضغط الملفات
- ✅ CDN Setup

#### الأسبوع 14-15: المزامنة

**Features:**
- ✅ مزامنة تلقائية
- ✅ حل التعارضات
- ✅ Background Sync
- ✅ قائمة انتظار المزامنة
- ✅ إشعارات المزامنة

**Database:**
- ✅ WatermelonDB Setup
- ✅ Sync Protocol
- ✅ Conflict Resolution Logic

---

### 📅 المرحلة 4: الإشعارات والميزات الإضافية (2-3 أسابيع)

#### الأسبوع 16-17: الإشعارات

**Features:**
- ✅ Push Notifications Setup
- ✅ التذكيرات اليومية
- ✅ إشعارات الكتب الجديدة
- ✅ إشعارات الإنجازات
- ✅ إعدادات الإشعارات

**Backend:**
- ✅ Expo Server SDK
- ✅ Notification Scheduling
- ✅ Device Registration

#### الأسبوع 18: الإحصائيات المتقدمة

**Features:**
- ✅ إحصائيات مفصلة
- ✅ رسوم بيانية
- ✅ Achievements System
- ✅ Streaks
- ✅ Goals

---

### 📅 المرحلة 5: الاختبار والتحسين (2-3 أسابيع)

#### الأسبوع 19-20: الاختبار الشامل

**Testing:**
- ✅ Unit Tests (80%+ Coverage)
- ✅ Integration Tests
- ✅ E2E Tests (Detox)
- ✅ Performance Testing
- ✅ اختبار على أجهزة حقيقية

**Devices:**
- iOS: iPhone SE, iPhone 14 Pro, iPad
- Android: Samsung S20, Pixel 6, Tablet

#### الأسبوع 21: التحسين والتلميع

**Optimization:**
- ✅ تحسين الأداء
- ✅ تقليل Bundle Size
- ✅ تحسين الصور
- ✅ تحسين الأنيميشن
- ✅ إصلاح الباغات

**Polish:**
- ✅ تحسين UI/UX
- ✅ إضافة الأنيميشن النهائية
- ✅ مراجعة النصوص
- ✅ التأكد من الترجمة

---

### 📅 المرحلة 6: النشر (1-2 أسبوع)

#### الأسبوع 22-23: الإصدار

**Preparation:**
- ✅ App Store Assets (Screenshots, Description)
- ✅ Privacy Policy & Terms
- ✅ Beta Testing (TestFlight & Google Play Beta)
- ✅ Marketing Materials
- ✅ App Store Optimization (ASO)

**Launch:**
- ✅ Submit to App Store
- ✅ Submit to Google Play
- ✅ إعداد Analytics
- ✅ إعداد Crash Reporting
- ✅ إعداد الدعم الفني

---

## 14. الاختبار

### 🧪 استراتيجية الاختبار

#### 1. Unit Testing (Jest)

**مثال:**
```typescript
// __tests__/utils/validators.test.ts
import { isValidEmail, isStrongPassword } from '@/utils/validators';

describe('Validators', () => {
  describe('isValidEmail', () => {
    it('should return true for valid email', () => {
      expect(isValidEmail('user@example.com')).toBe(true);
    });

    it('should return false for invalid email', () => {
      expect(isValidEmail('invalid-email')).toBe(false);
    });
  });

  describe('isStrongPassword', () => {
    it('should return true for strong password', () => {
      expect(isStrongPassword('Strong@123')).toBe(true);
    });

    it('should return false for weak password', () => {
      expect(isStrongPassword('weak')).toBe(false);
    });
  });
});
```

#### 2. Component Testing (React Native Testing Library)

```typescript
// __tests__/components/BookCard.test.tsx
import { render, fireEvent } from '@testing-library/react-native';
import BookCard from '@/components/books/BookCard';

describe('BookCard', () => {
  const mockBook = {
    id: '1',
    title: 'Test Book',
    author: 'Test Author',
    coverImage: 'https://example.com/cover.jpg'
  };

  const mockOnPress = jest.fn();

  it('should render correctly', () => {
    const { getByText } = render(
      <BookCard book={mockBook} onPress={mockOnPress} />
    );

    expect(getByText('Test Book')).toBeTruthy();
    expect(getByText('Test Author')).toBeTruthy();
  });

  it('should call onPress when pressed', () => {
    const { getByTestId } = render(
      <BookCard book={mockBook} onPress={mockOnPress} />
    );

    fireEvent.press(getByTestId('book-card'));
    expect(mockOnPress).toHaveBeenCalledWith(mockBook.id);
  });
});
```

#### 3. E2E Testing (Detox)

```typescript
// e2e/login.test.ts
describe('Login Flow', () => {
  beforeAll(async () => {
    await device.launchApp();
  });

  it('should login successfully', async () => {
    await element(by.id('email-input'))
      .typeText('user@example.com');

    await element(by.id('password-input'))
      .typeText('password123');

    await element(by.id('login-button')).tap();

    await expect(element(by.id('home-screen')))
      .toBeVisible();
  });

  it('should show error for invalid credentials', async () => {
    await element(by.id('email-input'))
      .typeText('wrong@example.com');

    await element(by.id('password-input'))
      .typeText('wrongpass');

    await element(by.id('login-button')).tap();

    await expect(element(by.text('بيانات خاطئة')))
      .toBeVisible();
  });
});
```

#### 4. Performance Testing

**React Native Performance Monitor:**
```typescript
import { PerformanceObserver } from 'react-native-performance';

const observer = new PerformanceObserver((list) => {
  list.getEntries().forEach((entry) => {
    console.log(`${entry.name}: ${entry.duration}ms`);
  });
});

observer.observe({ entryTypes: ['measure'] });
```

**Measure Component Render:**
```typescript
import { startMeasure, endMeasure } from '@/utils/performance';

const BookList = () => {
  startMeasure('BookList-render');

  // Component logic...

  useEffect(() => {
    endMeasure('BookList-render');
  }, []);

  return <View>...</View>;
};
```

---

## 15. النشر

### 📱 نشر التطبيق

#### 1. إعداد EAS (Expo Application Services)

**التثبيت:**
```bash
npm install -g eas-cli
eas login
eas build:configure
```

**eas.json:**
```json
{
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "autoIncrement": true
    }
  }
}
```

#### 2. بناء التطبيق

**iOS:**
```bash
# Development Build
eas build --platform ios --profile development

# Production Build
eas build --platform ios --profile production
```

**Android:**
```bash
# Development Build
eas build --platform android --profile development

# Production Build
eas build --platform android --profile production
```

#### 3. App Store (iOS)

**المتطلبات:**
- حساب Apple Developer ($99/سنة)
- App Store Connect Access
- App Icon (1024x1024)
- Screenshots (مقاسات مختلفة)
- App Description
- Keywords
- Privacy Policy URL

**الخطوات:**
1. إنشاء App في App Store Connect
2. تحميل Build من EAS
3. ملء معلومات التطبيق
4. إضافة Screenshots
5. Submit for Review
6. الانتظار (1-7 أيام)

#### 4. Google Play (Android)

**المتطلبات:**
- حساب Google Play Console ($25 مرة واحدة)
- App Icon (512x512)
- Feature Graphic (1024x500)
- Screenshots
- App Description
- Privacy Policy URL

**الخطوات:**
1. إنشاء App في Play Console
2. تحميل AAB من EAS
3. ملء معلومات التطبيق
4. إضافة Screenshots
5. Submit for Review
6. الانتظار (1-3 أيام)

#### 5. OTA Updates (Expo Updates)

**إعداد:**
```bash
eas update:configure
```

**نشر تحديث:**
```bash
# تحديث للـ Production
eas update --branch production --message "إصلاح باغات"

# تحديث للـ Preview
eas update --branch preview --message "ميزات جديدة"
```

**في الكود:**
```typescript
import * as Updates from 'expo-updates';

// التحقق من التحديثات
const checkForUpdates = async () => {
  try {
    const update = await Updates.checkForUpdateAsync();

    if (update.isAvailable) {
      await Updates.fetchUpdateAsync();
      await Updates.reloadAsync(); // إعادة تشغيل
    }
  } catch (error) {
    console.error(error);
  }
};
```

---

## 16. الصيانة والتحديثات

### 🔧 خطة الصيانة

#### 1. Monitoring & Analytics

**Sentry (Crash Reporting):**
```typescript
import * as Sentry from '@sentry/react-native';

Sentry.init({
  dsn: process.env.SENTRY_DSN,
  enableInExpoDevelopment: false,
  debug: __DEV__,
});
```

**Analytics:**
```typescript
import * as Analytics from 'expo-firebase-analytics';

// تتبع الشاشات
Analytics.logEvent('screen_view', {
  screen_name: 'HomeScreen',
  screen_class: 'HomeScreen'
});

// تتبع الأحداث
Analytics.logEvent('book_opened', {
  book_id: bookId,
  book_title: bookTitle
});
```

#### 2. User Feedback

**In-App Feedback:**
```typescript
const FeedbackScreen = () => {
  const [feedback, setFeedback] = useState('');

  const submitFeedback = async () => {
    await API.submitFeedback({
      type: 'feature_request',
      message: feedback,
      appVersion: Constants.expoConfig.version,
      platform: Platform.OS
    });
  };

  return (
    <View>
      <TextInput
        value={feedback}
        onChangeText={setFeedback}
        placeholder="اكتب اقتراحك..."
        multiline
      />
      <Button onPress={submitFeedback}>
        إرسال
      </Button>
    </View>
  );
};
```

#### 3. التحديثات الدورية

**شهرياً:**
- ✅ تحديث المكتبات
- ✅ إصلاح الباغات
- ✅ تحسينات الأداء
- ✅ OTA Update

**ربع سنوي:**
- ✅ ميزات جديدة
- ✅ تحديث التصميم
- ✅ App Store Update

**سنوياً:**
- ✅ Refactoring
- ✅ تحديثات كبرى
- ✅ مراجعة شاملة

---

## 17. الجدول الزمني

### 📅 Timeline الكامل

```
الشهر 1-2: الأساسيات
├── الأسبوع 1-2: المشروع والبنية
├── الأسبوع 3-4: المصادقة
├── الأسبوع 5-6: تصفح الكتب
└── الأسبوع 7-8: القارئ (جزء 1)

الشهر 3: القراءة والمكتبة
├── الأسبوع 9-10: القارئ (جزء 2)
├── الأسبوع 11-12: المكتبة الشخصية
└── الأسبوع 13: الإحصائيات

الشهر 4: الأوفلاين والمزامنة
├── الأسبوع 14-15: التنزيلات
├── الأسبوع 16-17: المزامنة
└── الأسبوع 18: الإشعارات

الشهر 5-6: الاختبار والنشر
├── الأسبوع 19-20: الاختبار الشامل
├── الأسبوع 21-22: التحسين
└── الأسبوع 23-24: النشر
```

**إجمالي الوقت: 5-6 أشهر**

---

## 18. الموارد المطلوبة

### 👥 الفريق

**الحد الأدنى:**
- 1x Full-Stack Developer (React Native + Node.js)
- 1x UI/UX Designer
- 1x QA Tester

**المثالي:**
- 2x Mobile Developers (React Native)
- 1x Backend Developer (Node.js/Prisma)
- 1x UI/UX Designer
- 1x QA Tester
- 1x DevOps Engineer

### 💰 التكاليف

**الخدمات السحابية:**
- Database Hosting: $20-50/شهر
- CDN (Cloudflare): $20-100/شهر
- Push Notifications (Expo): $0-50/شهر
- Crash Reporting (Sentry): $26-80/شهر

**الأدوات:**
- Apple Developer: $99/سنة
- Google Play: $25 (مرة واحدة)
- Expo EAS: $29-99/شهر

**التطوير:**
- مطور واحد: $3000-6000/شهر
- مصمم: $2000-4000/شهر
- QA: $1500-3000/شهر

**التقدير الإجمالي للـ MVP: $25,000 - $50,000**

---

## 🎯 الخلاصة والخطوات التالية

### ✅ ما تم تغطيته في هذه الخطة

1. ✅ تحليل شامل للمشروع الحالي
2. ✅ اختيار التقنيات المناسبة (React Native + Expo)
3. ✅ تصميم قاعدة بيانات موسعة ومشتركة
4. ✅ تصميم API كامل ومفصل
5. ✅ هيكلة تطبيق الموبايل
6. ✅ تفصيل جميع الميزات المطلوبة
7. ✅ تصميم واجهة المستخدم (UI/UX)
8. ✅ استراتيجية الأمان الكاملة
9. ✅ نظام الأوفلاين والمزامنة
10. ✅ نظام الإشعارات
11. ✅ تحسينات الأداء
12. ✅ خطة تنفيذ مرحلية مفصلة
13. ✅ استراتيجية الاختبار
14. ✅ خطة النشر
15. ✅ خطة الصيانة
16. ✅ جدول زمني واقعي
17. ✅ تقدير الموارد والتكاليف

### 🚀 الخطوات التالية (غداً)

#### المرحلة 0: الإعداد (يوم 1)

1. **مراجعة الخطة**
   - قراءة الخطة كاملة
   - تحديد الأولويات
   - تعديل حسب الحاجة

2. **إعداد البيئة**
   - تثبيت Expo CLI
   - إنشاء مشروع جديد
   - إعداد Git Repository

3. **إعداد Backend**
   - مراجعة API الحالي
   - إضافة JWT
   - Migration لقاعدة البيانات

4. **البدء في التطوير**
   - إنشاء هيكل المشروع
   - إعداد Navigation
   - إنشاء أول شاشة

---

## 📚 مراجع ومصادر

### الوثائق الرسمية
- [React Native Documentation](https://reactnative.dev/docs/getting-started)
- [Expo Documentation](https://docs.expo.dev/)
- [React Navigation](https://reactnavigation.org/docs/getting-started)
- [Prisma Documentation](https://www.prisma.io/docs/)

### المكتبات المستخدمة
- [Zustand](https://docs.pmnd.rs/zustand/getting-started/introduction)
- [React Query](https://tanstack.com/query/latest/docs/react/overview)
- [WatermelonDB](https://watermelondb.dev/docs)
- [React Native Reanimated](https://docs.swmansion.com/react-native-reanimated/)

### دروس مفيدة
- [Full Stack React Native (Arabic)](https://www.youtube.com/...)
- [Expo EAS Build Tutorial](https://www.youtube.com/...)
- [React Native Performance Tips](https://www.youtube.com/...)

---

<div align="center">

**🎉 انتهت الخطة! جاهزون للبدء غداً إن شاء الله! 🚀**

---

**المكتبة الإسلامية**
**📱 تطبيق موبايل متكامل**

**2025**

</div>
