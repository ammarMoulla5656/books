# 📦 كيفية نقل المشروع إلى جهازك

## الطريقة 1: نسخ المجلد كاملاً

### 1. انسخ المجلد
```bash
# إذا كان على نفس الجهاز
cp -r algiers ~/Desktop/algiers

# أو باستخدام USB أو السحابة
# انسخ المجلد "algiers" كاملاً
```

### 2. على جهازك الجديد
```bash
cd algiers
npm install
npx prisma migrate deploy
npm run dev
```

### 3. أضف البيانات الأساسية
```bash
curl -X POST http://localhost:3000/api/admin/seed
```

---

## الطريقة 2: نقل مع البيانات الموجودة

إذا كانت لديك كتب مستخرجة بالفعل:

### 1. انسخ هذه الملفات بالذات
```
algiers/
├── prisma/dev.db          ← قاعدة البيانات بكل محتوياتها!
├── .env
├── package.json
└── جميع المجلدات الأخرى
```

### 2. على جهازك الجديد
```bash
cd algiers
npm install
npm run dev
```

✅ جاهز! كل البيانات والكتب موجودة!

---

## الطريقة 3: نقل فقط قاعدة البيانات

إذا أردت نقل البيانات فقط:

### 1. من الجهاز القديم
```bash
# انسخ الملف
cp prisma/dev.db ~/Desktop/algiers-database.db
```

### 2. إلى الجهاز الجديد
```bash
# الصق الملف
cp ~/Downloads/algiers-database.db algiers/prisma/dev.db
```

---

## 📏 حجم الملفات

```
المجلد بدون node_modules:     ~50 MB
node_modules:                   ~400 MB
قاعدة البيانات (فارغة):        ~100 KB
قاعدة البيانات (مع 12 كتاب):  ~20-50 MB
```

**نصيحة**: احذف `node_modules` قبل النقل، ثم شغّل `npm install` على الجهاز الجديد

---

## ✅ التحقق من النجاح

بعد النقل، شغّل:
```bash
npm run dev
```

افتح: http://localhost:3000

إذا رأيت الموقع → **نجح النقل!** ✅

---

## 🐛 حل المشاكل

### مشكلة: `prisma` not found
```bash
npm install
```

### مشكلة: Database does not exist
```bash
npx prisma migrate deploy
```

### مشكلة: لا توجد بيانات
```bash
curl -X POST http://localhost:3000/api/admin/seed
```

---

## 💾 نسخ احتياطي

للاحتفاظ بنسخة احتياطية:
```bash
cp prisma/dev.db prisma/dev.db.backup-$(date +%Y%m%d)
```

---

## 🎉 بهذه البساطة!

المشروع محلي بالكامل، يمكنك نقله إلى أي جهاز وسيعمل فوراً!
