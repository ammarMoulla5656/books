# 📱 بدء سريع - تطبيق الموبايل

## 🎯 الخطوات (بالترتيب)

### 1️⃣ تثبيت Expo
```bash
cd algiers-mobile
npm install expo@~54.0.0
```

انتظر حتى ينتهي التثبيت (قد يستغرق دقائق)

### 2️⃣ تثبيت باقي الحزم
```bash
npm install
```

### 3️⃣ شغّل Backend
في terminal منفصل:
```bash
cd ../algiers
npm run dev
```

### 4️⃣ حدّث عنوان API
احصل على IP جهازك:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
# مثال: 192.168.1.100
```

افتح `algiers-mobile/services/api.ts` وغيّر:
```typescript
const API_BASE_URL = __DEV__ 
  ? 'http://192.168.1.100:3000'  // ضع IP جهازك هنا
  : 'https://your-production-url.com';
```

### 5️⃣ شغّل التطبيق
```bash
npm start
```

أو:
```bash
npx expo start
```

### 6️⃣ على الموبايل
1. نزّل **Expo Go** من App Store أو Play Store
2. افتح التطبيق
3. امسح QR code من Terminal
4. التطبيق سيفتح!

---

## 🐛 إذا واجهت مشاكل

### مشكلة: Cannot find module
```bash
npm install
```

### مشكلة: SDK version mismatch
```bash
npm install expo@~54.0.0
```

### مشكلة: Connection refused
- تأكد أن Backend يعمل
- تأكد من IP الصحيح في `services/api.ts`
- تأكد أن الموبايل والكمبيوتر على نفس الشبكة

---

## ✅ الخلاصة

الترتيب:
1. ✅ تثبيت expo
2. ✅ تثبيت باقي الحزم
3. ✅ تشغيل Backend
4. ✅ تحديث IP
5. ✅ تشغيل التطبيق
6. ✅ مسح QR code

**بهذه البساطة!** 🎉
