# 🚀 ابدأ هنا - التشغيل السريع

**أنت الآن جاهز تقريباً!** فقط خطوة واحدة متبقية.

---

## 📋 ما تم إنجازه

- ✅ تثبيت PostgreSQL 18
- ✅ تحديث ملف `.env`
- ✅ المشروع جاهز للعمل

---

## 🎯 الخطوة المتبقية

### افتح pgAdmin 4 وأنشئ قاعدة البيانات

1. افتح **pgAdmin 4** (ابحث عنه في قائمة Start)
2. سيطلب منك كلمة المرور (التي أدخلتها عند تثبيت PostgreSQL)
3. بعد الدخول:
   - انقر بزر الماوس الأيمن على **"Databases"**
   - اختر **"Create"** → **"Database"**
   - في حقل **"Database"** اكتب: `islamic_library`
   - اضغط **"Save"**

**هذا كل شيء!** ✅

---

## 🚀 بعد ذلك شغل المشروع

افتح Terminal في مجلد المشروع وشغل:

### الخطوة 1: تطبيق المخطط
```bash
npx prisma db push
```

يجب أن ترى:
```
✔ Your database is now in sync with your Prisma schema.
```

### الخطوة 2: شغل Next.js
```bash
npm run dev
```

### الخطوة 3: أنشئ البيانات الأولية

افتح المتصفح واذهب إلى:
```
http://localhost:3000/api/admin/seed
```

يجب أن ترى رسالة نجاح JSON.

### الخطوة 4: سجل دخول

اذهب إلى:
```
http://localhost:3000/secret-admin-panel-xyz
```

**بيانات الدخول**:
- 📧 البريد: `admin@islamic-library.com`
- 🔑 كلمة المرور: `Admin@123456`

---

## 🎉 الآن المشروع يعمل!

بعد تسجيل الدخول:
1. اذهب لـ **"إدارة الكتب"**
2. اضغط **"إضافة كتاب جديد"**
3. املأ البيانات
4. احفظ

الكتب ستظهر في الصفحة الرئيسية فوراً! ✅

---

## 🆘 إذا واجهت مشكلة

راجع ملف [إعداد_PostgreSQL.md](إعداد_PostgreSQL.md) - يحتوي على حل لكل المشاكل الشائعة.

---

## 📝 ملاحظة مهمة

**ملف `.env` الحالي**:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/islamic_library"
```

إذا كانت كلمة مرور PostgreSQL مختلفة، عدّل الجزء الثاني (`postgres:postgres`):
```
DATABASE_URL="postgresql://postgres:كلمة_المرور_هنا@localhost:5432/islamic_library"
```

---

**ذلك كل شيء! فقط أنشئ قاعدة البيانات في pgAdmin وشغل الأوامر أعلاه.** 🚀

**وقت القراءة**: دقيقة واحدة
**وقت التنفيذ**: 3 دقائق
