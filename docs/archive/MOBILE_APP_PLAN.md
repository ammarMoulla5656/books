# 📱 خطة تطبيق الموبايل - المكتبة الإسلامية

## 🎯 الهدف

إنشاء تطبيق موبايل (iOS & Android) يشترك مع الموقع في:
- ✅ نفس قاعدة البيانات
- ✅ نفس الـ Backend APIs
- ✅ بيانات Admin مشتركة (التصنيفات، الكتب، المحتوى)
- ❌ الإشارات المرجعية منفصلة (كل جهاز له إشاراته الخاصة)

---

## 🏗️ البنية المعمارية

```
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL Database                       │
│  (قاعدة بيانات مشتركة)                                      │
└─────────────────────────────────────────────────────────────┘
                           ▲
                           │
┌──────────────────────────┼──────────────────────────────────┐
│                   Next.js Backend APIs                       │
│  /api/books, /api/categories, /api/admin/*                  │
└──────────────────────────┬──────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  Next.js Web │  │ React Native │  │ React Native │
│   (Desktop)  │  │    (iOS)     │  │  (Android)   │
└──────────────┘  └──────────────┘  └──────────────┘
      │                  │                  │
      ▼                  ▼                  ▼
  localStorage      AsyncStorage       AsyncStorage
  (Bookmarks)       (Bookmarks)        (Bookmarks)
```

---

## 📊 البيانات المشتركة vs المنفصلة

### ✅ مشتركة (من Backend):
- الكتب والمحتوى
- التصنيفات
- الفصول والأقسام
- بيانات Admin
- الإعدادات العامة

### ❌ منفصلة (محلية):
- الإشارات المرجعية (Bookmarks)
- التظليلات (Highlights)
- إعدادات القراءة الشخصية
- السجل (History)

---

## 🛠️ التقنيات المستخدمة

### للموبايل:
- **React Native** - إطار العمل
- **Expo** - أدوات التطوير والبناء
- **React Navigation** - التنقل بين الشاشات
- **AsyncStorage** - التخزين المحلي
- **Axios** - طلبات API
- **React Query** - إدارة الحالة والـ Cache

### Backend الحالي:
- **Next.js 16** - يبقى كما هو
- **PostgreSQL** - قاعدة البيانات المشتركة
- **Prisma ORM** - يبقى كما هو

---

## 📁 هيكل المشروع

```
algiers/                          # المشروع الحالي (Web + Backend)
├── app/                          # Next.js pages & APIs
├── components/                   # React components
├── lib/                          # Utilities
└── prisma/                       # Database schema

algiers-mobile/                   # تطبيق الموبايل الجديد
├── app/                          # Expo Router pages
├── components/                   # React Native components
├── services/                     # API clients
│   └── api.ts                   # يتصل بـ Backend
├── store/                        # State management
├── utils/                        # Utilities
└── app.json                     # Expo config
```

---

## 🔌 نقاط الاتصال (APIs)

التطبيق سيستخدم نفس الـ APIs الموجودة:

### للقراءة (مشتركة):
```
GET  /api/books              - قائمة الكتب
GET  /api/books/[id]         - تفاصيل كتاب
GET  /api/categories         - التصنيفات
GET  /api/books/[id]/chapters - فصول الكتاب
```

### للإشارات المرجعية (محلية):
```javascript
// في الموبايل: AsyncStorage
await AsyncStorage.setItem('bookmarks', JSON.stringify(bookmarks));

// في الويب: localStorage
localStorage.setItem('bookmarks', JSON.stringify(bookmarks));
```

---

## 🎨 الشاشات الرئيسية

### 1. الصفحة الرئيسية (Home)
- قائمة التصنيفات
- الكتب المميزة
- البحث

### 2. التصنيفات (Categories)
- عرض جميع التصنيفات
- الانتقال للكتب

### 3. الكتب (Books)
- قائمة الكتب في تصنيف
- البحث والفلترة

### 4. قراءة الكتاب (Reader)
- عرض الفصول
- التنقل بين الأقسام
- إضافة إشارات مرجعية
- تظليل النصوص

### 5. الإشارات المرجعية (Bookmarks)
- قائمة الإشارات المحفوظة محلياً
- الانتقال السريع

### 6. الإعدادات (Settings)
- حجم الخط
- الوضع الليلي
- إعدادات القراءة

---

## 🚀 خطوات التنفيذ

### المرحلة 1: الإعداد (30 دقيقة)
- ✅ إنشاء مشروع React Native + Expo
- ✅ إعداد Navigation
- ✅ إعداد API client

### المرحلة 2: الشاشات الأساسية (2-3 ساعات)
- ✅ الصفحة الرئيسية
- ✅ قائمة الكتب
- ✅ شاشة القراءة

### المرحلة 3: المميزات (2-3 ساعات)
- ✅ الإشارات المرجعية (محلية)
- ✅ البحث
- ✅ الإعدادات
- ✅ الوضع الليلي

### المرحلة 4: التحسينات (1-2 ساعة)
- ✅ التصميم الإسلامي
- ✅ الرسوم المتحركة
- ✅ الأداء

---

## 🔄 التزامن

### البيانات المشتركة (تلقائية):
- الموبايل يقرأ من نفس Backend
- أي تغيير في Admin يظهر فوراً في الموبايل
- لا حاجة لتزامن يدوي

### البيانات المحلية (منفصلة):
- كل جهاز يحتفظ بإشاراته محلياً
- لا تزامن بين الأجهزة (كما طلبت)

---

## 📱 متطلبات التشغيل

### للتطوير:
```bash
# تثبيت Expo CLI
npm install -g expo-cli

# تثبيت Expo Go على الموبايل
# iOS: App Store
# Android: Play Store
```

### للإنتاج:
```bash
# بناء للـ iOS
eas build --platform ios

# بناء للـ Android
eas build --platform android
```

---

## 💡 المميزات الإضافية (اختيارية)

- 📥 تحميل الكتب للقراءة بدون إنترنت
- 🔔 إشعارات للكتب الجديدة
- 🎙️ قراءة صوتية
- 📊 إحصائيات القراءة
- 🌍 مشاركة الاقتباسات

---

## 🎯 الخلاصة

التطبيق سيكون:
- ✅ منفصل تماماً عن الـ Web
- ✅ يستخدم نفس Backend و Database
- ✅ الإشارات المرجعية محلية لكل جهاز
- ✅ Admin مشترك (التصنيفات والكتب)
- ✅ سريع وسهل الاستخدام

---

**جاهز للبدء!** 🚀
