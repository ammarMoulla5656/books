# 📚 دليل الاستخراج المحسّن للكتب | Enhanced Book Scraping Guide

**التاريخ**: 3 يناير 2026

---

## ✨ المميزات الجديدة

### 1. ✅ إزالة العبارات غير المطلوبة
- ❌ "العربية فارسی اردو English Azərbaycan Türkçe Français"
- ❌ "موقع مكتب سماحة المرجع الديني..."
- ❌ عناصر Navigation و Headers
- ✅ محتوى نظيف 100%

### 2. 🎨 تنسيق بالألوان التلقائي

#### أ) مسألة 1، مسألة 2 → 🟢 أخضر
```html
<span class="text-green-600 dark:text-green-400 font-bold">مسألة 1:</span>
```

#### ب) الأحوط → 🟡 أصفر مع خلفية
```html
<span class="text-yellow-600 dark:text-yellow-400 font-semibold bg-yellow-50 dark:bg-yellow-900/20 px-1 rounded">الأحوط</span>
```

#### ج) النصوص الدينية → 🔵 أزرق
```html
<span class="text-blue-600 dark:text-blue-400 text-sm">(صلى الله عليه وآله)</span>
<span class="text-blue-600 dark:text-blue-400 text-sm">(عليه السلام)</span>
```

### 3. 📖 بنية هرمية منظمة

```
كتاب الحج (فصل رئيسي)
├── واجبات الحج (قسم)
│   ├── مسألة 1: الإحرام...
│   ├── مسألة 2: الطواف...
│   └── مسألة 3: السعي...
├── مستحبات الحج (قسم)
│   ├── مسألة 1: الغسل...
│   └── مسألة 2: الدعاء...
```

### 4. 🔍 استخراج المسائل الكاملة
- ✅ استخراج كل مسألة بشكل منفصل
- ✅ ترقيم تلقائي
- ✅ محتوى كامل لكل مسألة

---

## 🚀 كيفية الاستخدام

### الطريقة 1: استخراج كتاب واحد

```bash
# استخراج "المسائل المنتخبة" مع 50 فصل
curl -X POST http://localhost:3000/api/admin/scrape-book-enhanced/13 \
  -H "Content-Type: application/json" \
  -d '{"maxChapters": 50}'
```

### الطريقة 2: استخراج جميع الكتب

احفظ هذا كـ `import-all-books-enhanced.sh`:

```bash
#!/bin/bash
echo "🕌 بسم الله الرحمن الرحيم"
echo "📚 استخراج محسّن لجميع كتب المرجع السيستاني"
echo ""

# قائمة الكتب مع عدد الفصول المتوقع
declare -A BOOKS
BOOKS[13]="المسائل المنتخبة:200"
BOOKS[14]="مناسك الحج وملحقاتها:80"
BOOKS[15]="منهاج الصالحين ج2:150"
BOOKS[16]="منهاج الصالحين ج3:120"
BOOKS[17]="الفقه للمغتربين:100"
BOOKS[18]="الميسر في الحج:60"
BOOKS[19]="الفتاوى الميسرة:150"
BOOKS[22]="التعليقة ج1:200"
BOOKS[23]="التعليقة ج2:200"
BOOKS[24]="الوجيز:100"
BOOKS[23720]="منهاج الصالحين ج1:180"
BOOKS[26278]="الصيام جنة:50"

TOTAL=${#BOOKS[@]}
CURRENT=0

for BOOK_ID in "${!BOOKS[@]}"; do
  CURRENT=$((CURRENT + 1))
  IFS=: read -r BOOK_NAME MAX_CHAPTERS <<< "${BOOKS[$BOOK_ID]}"

  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "[$CURRENT/$TOTAL] 📖 $BOOK_NAME (ID: $BOOK_ID)"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  RESULT=$(curl -X POST "http://localhost:3000/api/admin/scrape-book-enhanced/$BOOK_ID" \
    -H "Content-Type: application/json" \
    -d "{\"maxChapters\": $MAX_CHAPTERS}" \
    -s)

  echo "$RESULT" | jq -r '
    if .success then
      "✅ نجح الاستخراج:\n   📑 الفصول: \(.book.chaptersCount)\n   📝 الأقسام: \(.book.sectionsCount)\n   ✏️  المسائل: \(.book.masailCount)"
    else
      "❌ فشل: \(.error)"
    end
  '

  echo ""
  echo "⏳ انتظار 15 ثانية قبل الكتاب التالي..."
  sleep 15
done

echo ""
echo "═══════════════════════════════════════════════"
echo "🎉 اكتمل استخراج جميع الكتب!"
echo "═══════════════════════════════════════════════"
echo ""
echo "📊 للتحقق من النتائج:"
echo "   🌐 افتح: http://localhost:3000"
echo "   🗄️  أو: npx prisma studio"
```

**التشغيل**:
```bash
chmod +x import-all-books-enhanced.sh
./import-all-books-enhanced.sh
```

---

## 📊 مثال على النتيجة

### قبل التحسين ❌
```
محتوى غير منظم مع عبارات زائدة...
العربية فارسی اردو English...
مسألة 1 بدون تنسيق...
```

### بعد التحسين ✅
```html
<span class="text-green-600 font-bold">مسألة 1:</span>
يجب على المكلف أن يتبع
<span class="text-yellow-600 bg-yellow-50">الأحوط</span>
في العبادات، وإذا شك في النبي
<span class="text-blue-600">(صلى الله عليه وآله)</span>
فعليه اليقين.
```

### النتيجة المرئية:
- **مسألة 1:** (بالأخضر)
- يجب على المكلف أن يتبع **الأحوط** (بالأصفر مع خلفية)
- في العبادات، وإذا شك في النبي **(صلى الله عليه وآله)** (بالأزرق)
- فعليه اليقين.

---

## 🎯 الإحصائيات

### كتاب تم استخراجه مؤخراً:
**مناسك الحج وملحقاتها**
- ✅ 15 فصل رئيسي
- ✅ 206 قسم
- ✅ 204 مسألة مستخرجة
- ✅ تنسيق بالألوان
- ✅ محتوى نظيف

---

## 🔧 الإعدادات المتقدمة

### 1. تغيير حد المحتوى

**الحالي**: 50,000 حرف لكل قسم

**لإزالة الحد** (في `route.ts`):
```typescript
// السطر 225 تقريباً
content: formatContent(masala.content) // بدون substring
```

### 2. تخصيص الألوان

في دالة `formatContent()`:

```typescript
// تغيير لون المسائل من أخضر إلى أزرق
.replace(
  /(مسألة\s*[٠-٩0-9]+\s*[:：]?)/g,
  '<span class="text-blue-600 dark:text-blue-400 font-bold">$1</span>'
)

// تغيير لون "الأحوط" من أصفر إلى برتقالي
.replace(
  /(الأحوط|على الأحوط)/g,
  '<span class="text-orange-600 dark:text-orange-400">$1</span>'
)
```

### 3. إضافة تنسيقات جديدة

مثال: تلوين "يجب" و "يستحب":

```typescript
// في دالة formatContent()
content = content.replace(
  /\b(يجب|الواجب|يلزم)\b/g,
  '<span class="text-red-600 dark:text-red-400 font-semibold">$1</span>'
);

content = content.replace(
  /\b(يستحب|المستحب|يُستحب)\b/g,
  '<span class="text-green-600 dark:text-green-400">$1</span>'
);
```

---

## 📝 التحقق من الجودة

### 1. فحص محتوى الكتاب
```bash
# الحصول على كتاب معين
curl -s "http://localhost:3000/api/books/BOOK_ID" | jq '.chapters[0].sections[0].content' | head -20
```

### 2. عد المسائل
```bash
# عد المسائل في الكتاب
curl -s "http://localhost:3000/api/books/BOOK_ID" | \
  jq '[.chapters[].sections[].content] | map(scan("مسألة [٠-٩0-9]+")) | length'
```

### 3. التحقق من التنسيق
```bash
# التحقق من وجود span tags للتنسيق
curl -s "http://localhost:3000/api/books/BOOK_ID" | \
  jq '.chapters[0].sections[0].content' | \
  grep -o '<span class=' | wc -l
```

---

## 🐛 حل المشاكل

### مشكلة 1: العبارات الزائدة لا تزال موجودة
**الحل**: تأكد من استخدام endpoint الجديد:
```bash
# ❌ القديم
/api/admin/scrape-book/13

# ✅ الجديد
/api/admin/scrape-book-enhanced/13
```

### مشكلة 2: المسائل غير ظاهرة
**الحل**: تحقق من regex في دالة `extractMasail()`:
```typescript
// يدعم الأرقام العربية والإنجليزية
/(?:مسألة|المسألة)\s*([٠-٩0-9]+)/
```

### مشكلة 3: الألوان لا تظهر في المتصفح
**الحل**: تأكد من أن Tailwind CSS يتضمن الـ classes:
```javascript
// في tailwind.config.js
content: [
  './app/**/*.{js,ts,jsx,tsx}',
  './components/**/*.{js,ts,jsx,tsx}',
],
```

---

## 📚 الكتب الجاهزة للاستخراج

| ID | الكتاب | الحالة | الفصول المتوقعة |
|---|---|---|---|
| 13 | المسائل المنتخبة | ⏳ جاهز | ~200 |
| 14 | مناسك الحج | ✅ تم (15 فصل) | ~80 |
| 15 | منهاج الصالحين ج2 | ⏳ جاهز | ~150 |
| 16 | منهاج الصالحين ج3 | ⏳ جاهز | ~120 |
| 17 | الفقه للمغتربين | ⏳ جاهز | ~100 |
| 18 | الميسّر في الحج | ⏳ جاهز | ~60 |
| 19 | الفتاوى الميسّرة | ⏳ جاهز | ~150 |
| 22 | التعليقة ج1 | ⏳ جاهز | ~200 |
| 23 | التعليقة ج2 | ⏳ جاهز | ~200 |
| 24 | الوجيز | ⏳ جاهز | ~100 |
| 23720 | منهاج الصالحين ج1 | ⏳ جاهز | ~180 |
| 26278 | الصيام جُنة | ⏳ جاهز | ~50 |

---

## ✅ الخلاصة

تم إنشاء نظام استخراج محسّن بالكامل يوفر:

✅ **محتوى نظيف 100%** - بدون عبارات زائدة
✅ **تنسيق ملون تلقائي** - مسائل، الأحوط، نصوص دينية
✅ **بنية هرمية منظمة** - فصول → أقسام → مسائل
✅ **استخراج كامل** - جميع المسائل والتفاصيل
✅ **حفظ مباشر** - في قاعدة البيانات مع التنسيق

**الآن يمكنك استخراج أي كتاب بجودة عالية وتنسيق جميل!**

---

**🕌 بسم الله الرحمن الرحيم**

**نظام الاستخراج المحسّن جاهز للاستخدام!**

*Last Updated: January 3, 2026*
