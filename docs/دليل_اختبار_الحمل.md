# 🧪 دليل اختبار الحمل - المكتبة الإسلامية

هذا الدليل يشرح كيفية استخدام أداة اختبار الحمل لمعرفة كم مستخدم يتحمل الموقع.

---

## 📋 المتطلبات

### 1. Python 3
تأكد من تثبيت Python 3:
```bash
python --version
```

يجب أن ترى: `Python 3.x.x`

### 2. مكتبة requests
```bash
pip install requests
```

---

## 🚀 طريقة الاستخدام

### الطريقة الأساسية

```bash
python load_test.py
```

سيحاكي **10 مستخدمين** لمدة **60 ثانية**.

---

## ⚙️ تخصيص الإعدادات

### تعديل عدد المستخدمين

افتح ملف `load_test.py` وغيّر هذا السطر:

```python
NUM_USERS = 10  # غيّر هذا الرقم
```

**أمثلة**:
- `NUM_USERS = 5` - 5 مستخدمين (اختبار خفيف)
- `NUM_USERS = 50` - 50 مستخدم (اختبار متوسط)
- `NUM_USERS = 100` - 100 مستخدم (اختبار ثقيل)
- `NUM_USERS = 500` - 500 مستخدم (اختبار شديد)

### تعديل مدة الاختبار

```python
DURATION_SECONDS = 60  # المدة بالثواني
```

**أمثلة**:
- `DURATION_SECONDS = 30` - 30 ثانية (سريع)
- `DURATION_SECONDS = 120` - دقيقتان
- `DURATION_SECONDS = 300` - 5 دقائق (اختبار طويل)

### تعديل التأخير بين الطلبات

```python
DELAY_BETWEEN_REQUESTS = 0.5  # نصف ثانية
```

**أمثلة**:
- `DELAY_BETWEEN_REQUESTS = 0.1` - أسرع (حمل أكبر)
- `DELAY_BETWEEN_REQUESTS = 1.0` - أبطأ (حمل أقل)

---

## 📊 فهم النتائج

### مثال على الناتج:

```
📊 إحصائيات اختبار الحمل
======================================================================

📈 إجمالي الطلبات:
   • الإجمالي:     450
   • ناجحة:        445 (98.9%)
   • فاشلة:        5 (1.1%)

⏱️  أوقات الاستجابة:
   • المتوسط:      0.234s
   • الأقل:        0.089s
   • الأكثر:       1.234s

🔗 الطلبات حسب المسار:
   • /api/books: 150
   • /: 100
   • /api/categories: 100
   • /books/[id]: 100

📊 معدل الطلبات: 7.5 طلب/ثانية

🎯 تقييم الأداء:
   ✅ ممتاز! الموقع يتحمل 10 مستخدم بدون مشاكل
```

---

## 📈 معايير الأداء

### وقت الاستجابة المتوسط:
- ✅ **أقل من 0.5 ثانية**: ممتاز
- ✔️  **0.5 - 1.0 ثانية**: جيد
- ⚠️  **1.0 - 2.0 ثانية**: مقبول
- ❌ **أكثر من 2.0 ثانية**: يحتاج تحسين

### معدل النجاح:
- ✅ **95-100%**: ممتاز
- ✔️  **90-95%**: جيد
- ⚠️  **80-90%**: مقبول
- ❌ **أقل من 80%**: مشاكل

---

## 🎯 سيناريوهات الاختبار

### 1. اختبار خفيف (5 مستخدمين)
```python
NUM_USERS = 5
DURATION_SECONDS = 30
```
**الاستخدام**: اختبار سريع للتحقق من أن كل شيء يعمل

### 2. اختبار عادي (20 مستخدم)
```python
NUM_USERS = 20
DURATION_SECONDS = 60
```
**الاستخدام**: محاكاة استخدام يومي عادي

### 3. اختبار متوسط (50 مستخدم)
```python
NUM_USERS = 50
DURATION_SECONDS = 120
```
**الاستخدام**: اختبار أوقات الذروة

### 4. اختبار ثقيل (100 مستخدم)
```python
NUM_USERS = 100
DURATION_SECONDS = 180
```
**الاستخدام**: معرفة الحد الأقصى

### 5. اختبار شديد (200+ مستخدم)
```python
NUM_USERS = 200
DURATION_SECONDS = 300
```
**الاستخدام**: اختبار الضغط القصوى

---

## 🔍 ماذا يفعل الاختبار؟

كل مستخدم افتراضي يقوم بـ:

1. **زيارة الصفحة الرئيسية** (`/`)
2. **جلب قائمة الكتب** (`/api/books`)
3. **اختيار كتاب عشوائي** وقراءته (`/books/[id]`)
4. **جلب تفاصيل الكتاب** (`/api/books/[id]`)
5. **جلب التصنيفات** (`/api/categories`)
6. **الانتظار** (1-3 ثواني)
7. **تكرار** العملية حتى انتهاء المدة

---

## 📝 أمثلة على الاستخدام

### مثال 1: اختبار سريع
```bash
# عدّل في load_test.py:
NUM_USERS = 5
DURATION_SECONDS = 30

# شغّل:
python load_test.py
```

### مثال 2: معرفة الحد الأقصى
```bash
# جرب بالتدريج:
# 1. شغّل مع 10 مستخدمين
NUM_USERS = 10
python load_test.py

# 2. إذا نجح، جرب 20
NUM_USERS = 20
python load_test.py

# 3. استمر في الزيادة حتى تبدأ المشاكل
NUM_USERS = 50
python load_test.py
```

### مثال 3: اختبار استمرارية
```bash
# اختبار طويل مع عدد معقول
NUM_USERS = 30
DURATION_SECONDS = 600  # 10 دقائق
python load_test.py
```

---

## ⚠️ ملاحظات مهمة

### 1. تأكد من تشغيل الموقع
```bash
npm run dev
```
يجب أن يكون الموقع على http://localhost:3000

### 2. أضف بعض الكتب أولاً
- سجل دخول للوحة التحكم
- أضف على الأقل 3-5 كتب
- الاختبار سيختار كتب عشوائية للقراءة

### 3. لا تبالغ في عدد المستخدمين
- ابدأ بأعداد صغيرة (10-20)
- زد تدريجياً
- جهازك قد لا يتحمل أرقام كبيرة جداً

### 4. إيقاف الاختبار
اضغط `Ctrl + C` في أي وقت لإيقاف الاختبار

---

## 🐛 حل المشاكل

### خطأ: "No module named 'requests'"
**الحل**:
```bash
pip install requests
```

### خطأ: "Connection refused"
**الحل**: تأكد من تشغيل الموقع:
```bash
npm run dev
```

### الطلبات كلها تفشل
**الحل**: تحقق من:
1. الموقع يعمل على http://localhost:3000
2. قاعدة البيانات متصلة
3. يوجد كتب في قاعدة البيانات

---

## 📊 مثال على اختبار تدريجي

```bash
# الخطوة 1: اختبار أساسي
NUM_USERS = 10
python load_test.py
# النتيجة: ✅ ممتاز

# الخطوة 2: زيادة الحمل
NUM_USERS = 25
python load_test.py
# النتيجة: ✅ جيد

# الخطوة 3: المزيد
NUM_USERS = 50
python load_test.py
# النتيجة: ⚠️ متوسط - بدأ البطء

# الخطوة 4: الحد الأقصى
NUM_USERS = 75
python load_test.py
# النتيجة: ❌ ضعيف - طلبات تفشل

# الخلاصة: الموقع يتحمل بشكل جيد حتى 50 مستخدم
```

---

## 🎯 التوصيات

### للتطوير المحلي:
- اختبر مع **10-20 مستخدم**
- مدة **30-60 ثانية**

### قبل النشر:
- اختبر مع **50-100 مستخدم**
- مدة **3-5 دقائق**

### بعد النشر:
- اختبر مع **100-200 مستخدم**
- راقب الأداء الحقيقي

---

## 📞 معلومات إضافية

**الملف**: `load_test.py`
**اللغة**: Python 3
**المكتبات**: requests (فقط)

**الاستخدامات**:
- معرفة عدد المستخدمين المدعوم
- اختبار الأداء تحت الضغط
- اكتشاف نقاط الضعف
- قياس أوقات الاستجابة

---

**وقت الإعداد**: دقيقة واحدة
**وقت الاختبار**: يعتمد على الإعدادات (30 ثانية - 10 دقائق)

تم التوثيق بـ ❤️ لمساعدتك
