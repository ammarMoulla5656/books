# ✅ تعليمات التشغيل - المكتبة الإسلامية

## 🚀 كيفية التشغيل

### الخطوة 1: تشغيل قاعدة البيانات (مرة واحدة فقط)

افتح نافذة طرفية (Terminal) جديدة واترك ها تعمل:
```bash
npx prisma dev
```

**ملاحظة**: اترك هذه النافذة مفتوحة طوال عملك على المشروع.

---

### الخطوة 2: تشغيل الموقع

في نافذة طرفية ثانية:
```bash
npm run dev
```

---

### الخطوة 3: إنشاء البيانات الأولية

افتح المتصفح واذهب إلى:
```
http://localhost:3000/api/admin/seed
```

ستظهر لك رسالة JSON بنجاح إنشاء:
- حساب Admin
- 4 تصنيفات أساسية

---

### الخطوة 4: تسجيل الدخول

اذهب إلى لوحة التحكم:
```
http://localhost:3000/secret-admin-panel-xyz
```

**بيانات تسجيل الدخول**:
- 📧 البريد: `admin@islamic-library.com`
- 🔑 كلمة المرور: `Admin@123456`

---

## ❌ حل المشاكل

### المشكلة: الكتب لا تظهر في الصفحة الرئيسية
**السبب**: قاعدة البيانات فارغة
**الحل**: أضف كتب من لوحة التحكم:
1. سجل دخول للأدمن
2. اذهب لـ "إدارة الكتب"
3. اضغط "إضافة كتاب جديد"
4. املأ البيانات واحفظ

### المشكلة: لا يمكن تسجيل الدخول
**الأسباب المحتملة**:
1. لم يتم إنشاء البيانات الأولية → اذهب للخطوة 3
2. قاعدة البيانات غير متصلة → تحقق من الخطوة 1
3. كلمة المرور خاطئة → استخدم `Admin@123456`

### المشكلة: خطأ في الاتصال بقاعدة البيانات
**الحل**:
1. أوقف Next.js (Ctrl+C)
2. شغل `npx prisma dev` في نافذة منفصلة
3. انتظر حتى تظهر "Your prisma dev server is ready"
4. شغل Next.js مرة أخرى: `npm run dev`

---

## 📝 ملاحظات مهمة

1. ✅ قاعدة البيانات المستخدمة: **Prisma Dev (PostgreSQL محلي)**
2. ✅ المنافذ المستخدمة: `51214` لقاعدة البيانات، `3000` للموقع
3. ✅ البيانات محفوظة بشكل دائم في قاعدة البيانات
4. ✅ جميع التعديلات تطبق على جميع المستخدمين

---

## 🎯 الخطوات المختصرة

### للمرة الأولى:
```bash
# Terminal 1
npx prisma dev

# Terminal 2
npm run dev

# ثم افتح في المتصفح
http://localhost:3000/api/admin/seed
```

### المرات القادمة:
```bash
# Terminal 1
npx prisma dev

# Terminal 2
npm run dev

# افتح الموقع
http://localhost:3000
```

---

## 🌐 الروابط المهمة

- **الصفحة الرئيسية**: http://localhost:3000
- **لوحة التحكم**: http://localhost:3000/secret-admin-panel-xyz
- **إنشاء البيانات الأولية**: http://localhost:3000/api/admin/seed
- **معلومات عن الموقع**: http://localhost:3000/about
- **اتصل بنا**: http://localhost:3000/contact

---

**آخر تحديث**: 9 يناير 2026
**الحالة**: ✅ جاهز للاستخدام
