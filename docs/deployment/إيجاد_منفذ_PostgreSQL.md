# 🔍 إيجاد منفذ PostgreSQL

**المشكلة**: Prisma لا يستطيع الاتصال بـ PostgreSQL على المنفذ 5432.

---

## 🎯 الحل السريع

### الطريقة 1: من خلال pgAdmin (الأسهل)

1. افتح **pgAdmin 4**
2. في الشريط الجانبي، انقر بزر الماوس الأيمن على **"PostgreSQL 18"**
3. اختر **"Properties"**
4. اذهب لتبويب **"Connection"**
5. ستجد **"Port"** - هذا هو المنفذ الصحيح!

**غالباً سيكون أحد هذه**:
- `5432` (الافتراضي)
- `5433`
- `5434`

---

### الطريقة 2: من خلال ملف الإعدادات

1. افتح File Explorer
2. اذهب إلى:
   ```
   C:\Program Files\PostgreSQL\18\data
   ```
3. افتح ملف **`postgresql.conf`** بأي محرر نصوص
4. ابحث عن السطر الذي يبدأ بـ `port =`
5. ستجد رقم المنفذ بجانبه

---

### الطريقة 3: من خلال Services

1. اضغط `Win + R`
2. اكتب `services.msc` واضغط Enter
3. ابحث عن **"postgresql-x64-18"**
4. انقر عليه مرتين
5. في خانة **"Path to executable"** ابحث عن `-p` متبوعاً برقم - هذا هو المنفذ

---

## ✅ بعد إيجاد المنفذ

### حدّث ملف `.env`

افتح ملف `.env` في المشروع وحدّث السطر التالي:

**إذا كان المنفذ 5433 مثلاً**:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/islamic_library"
```

**إذا كان المنفذ 5434**:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5434/islamic_library"
```

---

## 🔧 حل بديل: تغيير منفذ PostgreSQL إلى 5432

إذا أردت استخدام المنفذ الافتراضي 5432:

### الخطوات:

1. افتح **pgAdmin 4**
2. أوقف الخادم (Server → Disconnect)
3. افتح ملف:
   ```
   C:\Program Files\PostgreSQL\18\data\postgresql.conf
   ```
4. ابحث عن السطر:
   ```
   port = 5433  # أو أي رقم آخر
   ```
5. غيّره إلى:
   ```
   port = 5432
   ```
6. احفظ الملف
7. أعد تشغيل خدمة PostgreSQL:
   - اضغط `Win + R`
   - اكتب `services.msc`
   - ابحث عن "postgresql-x64-18"
   - انقر بزر الماوس الأيمن → Restart

---

## 🆘 مشكلة شائعة: المنفذ 5432 مستخدم

إذا كان المنفذ 5432 مستخدماً من برنامج آخر:

### تحقق من البرامج المستخدمة للمنفذ:

```bash
netstat -ano | findstr :5432
```

إذا ظهر شيء، معناه أن برنامج آخر يستخدم هذا المنفذ.

### الحل:
استخدم منفذ مختلف (مثل 5433) وحدّث `.env` كما أوضحنا أعلاه.

---

## ✅ تأكيد النجاح

بعد تحديث `.env` بالمنفذ الصحيح، جرب:

```bash
npx prisma db push
```

يجب أن ترى:
```
✔ Your database is now in sync with your Prisma schema.
```

---

## 📝 أمثلة للـ DATABASE_URL

اختر الذي يناسبك:

### المنفذ 5432 (الافتراضي):
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/islamic_library"
```

### المنفذ 5433:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/islamic_library"
```

### المنفذ 5434:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5434/islamic_library"
```

### كلمة مرور مختلفة:
```
DATABASE_URL="postgresql://postgres:كلمة_المرور_هنا@localhost:5432/islamic_library"
```

---

## 🎯 الخلاصة

1. افتح **pgAdmin** وابحث عن المنفذ في Properties
2. حدّث ملف **`.env`** بالمنفذ الصحيح
3. شغل `npx prisma db push`
4. كل شيء سيعمل! ✅

---

**الوقت المتوقع**: 2 دقيقة
**سهولة التنفيذ**: ⭐⭐⭐⭐⭐

تم التوثيق بـ ❤️ لمساعدتك
