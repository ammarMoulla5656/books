# 🐘 إعداد PostgreSQL - المكتبة الإسلامية

**التاريخ**: 9 يناير 2026

---

## ✅ تم تثبيت PostgreSQL

لقد قمت بتثبيت PostgreSQL 18. الآن نحتاج إلى إعداده للمشروع.

---

## 🔧 خطوات الإعداد

### الخطوة 1: إنشاء قاعدة البيانات

افتح **pgAdmin 4** (البرنامج الذي جاء مع PostgreSQL):

1. افتح pgAdmin 4
2. اتصل بـ PostgreSQL (سيطلب كلمة المرور التي أدخلتها عند التثبيت)
3. انقر بزر الماوس الأيمن على "Databases"
4. اختر "Create" → "Database"
5. اكتب الاسم: `islamic_library`
6. اضغط "Save"

---

### الخطوة 2: تحديث كلمة المرور (إن لزم الأمر)

إذا كانت كلمة مرورك مختلفة عن `postgres`، حدّث ملف `.env`:

```
DATABASE_URL="postgresql://postgres:كلمة_المرور_هنا@localhost:5432/islamic_library"
```

---

### الخطوة 3: تطبيق المخطط

بعد إنشاء قاعدة البيانات في pgAdmin، ارجع للمشروع وشغل:

```bash
npx prisma db push
```

هذا سينشئ جميع الجداول (17 جدول).

---

### الخطوة 4: إنشاء البيانات الأولية

شغل Next.js:
```bash
npm run dev
```

افتح المتصفح:
```
http://localhost:3000/api/admin/seed
```

هذا سينشئ:
- حساب Admin
- 4 تصنيفات أساسية

---

### الخطوة 5: تسجيل الدخول

اذهب إلى:
```
http://localhost:3000/secret-admin-panel-xyz
```

البيانات:
- البريد: `admin@islamic-library.com`
- كلمة المرور: `Admin@123456`

---

## 🆘 حل المشاكل

### Problem: P1001 Can't reach database server

**الأسباب المحتملة**:
1. PostgreSQL غير مشغل
2. كلمة المرور خاطئة
3. قاعدة البيانات غير موجودة
4. المنفذ مختلف

**الحلول**:

#### 1. تأكد من تشغيل PostgreSQL
- افتح "Services" (اضغط Win+R واكتب `services.msc`)
- ابحث عن "postgresql-x64-18"
- تأكد من أنه "Running"
- إذا كان متوقفاً، انقر عليه واضغط "Start"

#### 2. تأكد من كلمة المرور
في pgAdmin، عند الاتصال، استخدم نفس كلمة المرور في `.env`

#### 3. تأكد من إنشاء قاعدة البيانات
في pgAdmin:
- انظر تحت "Databases"
- يجب أن ترى "islamic_library"
- إذا لم تكن موجودة، أنشئها

#### 4. تحقق من المنفذ
افتح pgAdmin → انقر على الخادم → Properties
انظر "Port" - يجب أن يكون 5432

إذا كان مختلفاً، حدّث `.env`:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:المنفذ_هنا/islamic_library"
```

---

## 📝 الطريقة البديلة (استخدام SQL مباشرة)

إذا واجهت مشاكل، يمكنك استخدام SQL مباشرة:

### في pgAdmin:

1. انقر على قاعدة البيانات "islamic_library"
2. اضغط على "Query Tool"
3. الصق هذا الأمر:

```sql
CREATE DATABASE islamic_library;
```

4. اضغط F5 أو زر "Execute"

---

## ✅ التحقق من النجاح

بعد إكمال الخطوات، جرب:

```bash
npx prisma db push
```

يجب أن ترى:
```
✔ Generated Prisma Client
Your database is now in sync with your Prisma schema.
```

إذا رأيت هذه الرسالة، كل شيء يعمل! ✅

---

## 🚀 الخطوات الكاملة المختصرة

```bash
# 1. افتح pgAdmin وأنشئ قاعدة بيانات "islamic_library"

# 2. في Terminal المشروع
npx prisma db push

# 3. شغل Next.js
npm run dev

# 4. افتح المتصفح
http://localhost:3000/api/admin/seed

# 5. سجل دخول
http://localhost:3000/secret-admin-panel-xyz
# البريد: admin@islamic-library.com
# كلمة المرور: Admin@123456
```

---

## 📞 معلومات مهمة

**ملف الإعدادات**: [.env](.env)
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/islamic_library"
```

**مكونات الرابط**:
- `postgres` (الأول): اسم المستخدم
- `postgres` (الثاني): كلمة المرور
- `localhost:5432`: عنوان الخادم والمنفذ
- `islamic_library`: اسم قاعدة البيانات

**غير هذه القيم حسب إعداداتك!**

---

## 🎯 الخلاصة

1. ✅ ثبت PostgreSQL → **تم**
2. ⏳ أنشئ قاعدة البيانات في pgAdmin → **قم بذلك الآن**
3. ⏳ شغل `npx prisma db push` → **بعد الخطوة 2**
4. ⏳ شغل `npm run dev` → **بعد الخطوة 3**
5. ⏳ افتح `/api/admin/seed` → **بعد الخطوة 4**

**بعد ذلك كل شيء سيعمل بشكل مثالي! 🎉**

---

تم التوثيق بـ ❤️ لمساعدتك
