# 🔧 حل مشكلة الاتصال بـ PostgreSQL

**المشكلة**: `P1001: Can't reach database server at localhost:5432`

---

## 🎯 الحل الكامل خطوة بخطوة

### السبب المحتمل
PostgreSQL مثبت ولكن إما:
1. لا يستمع على المنفذ الصحيح
2. لا يسمح بالاتصالات المحلية
3. كلمة المرور أو اسم المستخدم خاطئ

---

## ✅ الحل 1: استخدام pgAdmin لإيجاد المنفذ

### الخطوات:

1. **افتح pgAdmin 4**

2. **سجل دخول** (أدخل كلمة المرور الرئيسية لـ pgAdmin)

3. **في الشريط الجانبي الأيسر**:
   - ستجد "Servers"
   - تحتها "PostgreSQL 18" (أو رقم الإصدار)

4. **انقر بزر الماوس الأيمن على "PostgreSQL 18"**
   - اختر **"Properties"**

5. **في نافذة Properties**:
   - اذهب لتبويب **"Connection"**
   - ستجد:
     - **Host**: localhost
     - **Port**: (هذا المنفذ الذي نبحث عنه!)
     - **Username**: postgres

6. **انسخ رقم المنفذ**

7. **حدّث ملف `.env`** في المشروع:
   ```
   DATABASE_URL="postgresql://postgres:postgres@localhost:المنفذ_هنا/islamic_library"
   ```

---

## ✅ الحل 2: التحقق من ملف pg_hba.conf

قد يكون PostgreSQL لا يسمح بالاتصالات المحلية.

### الخطوات:

1. **افتح File Explorer**

2. **اذهب إلى**:
   ```
   C:\Program Files\PostgreSQL\18\data
   ```

3. **افتح ملف `pg_hba.conf`** بمحرر نصوص (مثل Notepad++)

4. **ابحث عن الأسطر** التي تبدأ بـ `host`:
   ```
   # IPv4 local connections:
   host    all             all             127.0.0.1/32            md5
   host    all             all             127.0.0.1/32            scram-sha-256
   ```

5. **تأكد من وجود هذا السطر**:
   ```
   host    all             all             127.0.0.1/32            trust
   ```
   أو
   ```
   host    all             all             127.0.0.1/32            md5
   ```

6. **إذا لم يكن موجوداً، أضفه**

7. **احفظ الملف**

8. **أعد تشغيل PostgreSQL**:
   - اضغط `Win + R`
   - اكتب `services.msc`
   - ابحث عن "postgresql-x64-18"
   - انقر بزر الماوس الأيمن → **Restart**

---

## ✅ الحل 3: استخدام اتصال مبسط بدون كلمة مرور (للتطوير فقط)

### تمكين اتصال trust للتطوير المحلي:

1. افتح `pg_hba.conf` (كما في الحل 2)

2. غيّر السطر من:
   ```
   host    all             all             127.0.0.1/32            scram-sha-256
   ```
   إلى:
   ```
   host    all             all             127.0.0.1/32            trust
   ```

3. احفظ وأعد تشغيل PostgreSQL

4. حدّث `.env`:
   ```
   DATABASE_URL="postgresql://postgres@localhost:5432/islamic_library"
   ```
   (لاحظ: بدون كلمة مرور)

---

## ✅ الحل 4: إنشاء قاعدة البيانات أولاً

قد تكون قاعدة البيانات غير موجودة!

### في pgAdmin:

1. **في الشريط الجانبي**، انقر بزر الماوس الأيمن على **"Databases"**

2. اختر **"Create" → "Database..."**

3. في حقل **"Database"** اكتب: `islamic_library`

4. اضغط **"Save"**

### الآن جرب:
```bash
npx prisma db push
```

---

## ✅ الحل 5: استخدام Supabase (البديل السهل)

إذا استمرت المشاكل، استخدم Supabase (مجاني وأسهل):

### الخطوات:

1. اذهب إلى https://supabase.com

2. أنشئ حساب مجاني

3. أنشئ مشروع جديد (New Project)

4. انتظر 2-3 دقائق حتى ينتهي الإعداد

5. اذهب إلى **Settings** → **Database**

6. انسخ **Connection String** (URI)

7. حدّث `.env`:
   ```
   DATABASE_URL="الرابط_المنسوخ_من_Supabase"
   ```

8. شغل:
   ```bash
   npx prisma db push
   ```

**ميزة Supabase**: لا حاجة لتثبيت أو إعداد أي شيء، كل شيء سحابي!

---

## 🔍 تشخيص المشكلة

### اختبر كل خطوة:

#### 1. هل PostgreSQL يعمل؟
```bash
sc query postgresql-x64-18
```
يجب أن ترى: `STATE: RUNNING`

#### 2. هل قاعدة البيانات موجودة؟
افتح pgAdmin → Databases → ابحث عن "islamic_library"

#### 3. هل المنفذ صحيح؟
في pgAdmin → PostgreSQL 18 → Properties → Connection → Port

#### 4. هل كلمة المرور صحيحة؟
جرب تسجيل الدخول في pgAdmin بنفس كلمة المرور

---

## 🎯 الحل السريع الموصى به

### الخيار أ: استخدام pgAdmin

1. افتح pgAdmin
2. أنشئ قاعدة بيانات "islamic_library"
3. تحقق من المنفذ في Properties
4. حدّث `.env` بالمنفذ الصحيح
5. شغل `npx prisma db push`

### الخيار ب: استخدام Supabase

1. اذهب لـ supabase.com
2. أنشئ مشروع
3. انسخ Connection String
4. ضعه في `.env`
5. شغل `npx prisma db push`

---

## 📝 أمثلة DATABASE_URL

### PostgreSQL محلي:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/islamic_library"
```

### PostgreSQL بمنفذ مختلف:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/islamic_library"
```

### PostgreSQL بدون كلمة مرور (trust):
```
DATABASE_URL="postgresql://postgres@localhost:5432/islamic_library"
```

### Supabase:
```
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT].supabase.co:5432/postgres"
```

---

## ✅ بعد الحل

عند نجاح الاتصال ستراى:

```bash
npx prisma db push
```

```
✔ Generated Prisma Client
Your database is now in sync with your Prisma schema. Done in 2.39s
```

---

**التوصية**: إذا استمرت المشاكل مع PostgreSQL المحلي، استخدم Supabase - أسهل بكثير! 🚀

تم التوثيق بـ ❤️ لمساعدتك
