# 🔧 إصلاح مشكلة ترميز قاعدة البيانات

## 🐛 المشكلة

```
character with byte sequence 0xd8 0xa7 in encoding "UTF8"
has no equivalent in encoding "WIN1252"
```

هذا الخطأ يحدث لأن PostgreSQL على Windows يستخدم ترميز WIN1252 بدلاً من UTF-8.

---

## ✅ الحل

### الطريقة 1: تعيين متغير البيئة (الأسرع)

#### في Windows:

1. افتح PowerShell كـ Administrator
2. نفّذ:

```powershell
[System.Environment]::SetEnvironmentVariable('PGCLIENTENCODING', 'UTF8', 'User')
```

3. أعد تشغيل VS Code أو Terminal

#### في .env.local:

أضف السطر التالي:

```env
PGCLIENTENCODING=UTF8
```

---

### الطريقة 2: تغيير إعدادات PostgreSQL (الأفضل)

#### الخطوة 1: تحديد موقع postgresql.conf

```bash
# في PostgreSQL command line
SHOW config_file;
```

#### الخطوة 2: تعديل الملف

أضف أو عدّل:

```conf
client_encoding = utf8
lc_messages = 'C'
lc_monetary = 'C'
lc_numeric = 'C'
lc_time = 'C'
```

#### الخطوة 3: إعادة تشغيل PostgreSQL

```bash
# في Services (خدمات Windows)
أوقف وشغّل خدمة PostgreSQL
```

---

### الطريقة 3: إنشاء قاعدة بيانات جديدة بترميز UTF-8

```sql
-- احذف القاعدة القديمة (احذر! سيحذف كل البيانات)
DROP DATABASE algiers;

-- أنشئ قاعدة جديدة بترميز UTF-8
CREATE DATABASE algiers
  WITH
  ENCODING = 'UTF8'
  LC_COLLATE = 'C'
  LC_CTYPE = 'C'
  TEMPLATE = template0;
```

ثم أعد تطبيق المخططات:

```bash
npx prisma db push
```

---

## 🧪 التحقق من الإصلاح

### اختبر الترميز:

```bash
# اتصل بقاعدة البيانات
psql -U postgres -d algiers

# تحقق من الترميز
SHOW server_encoding;
SHOW client_encoding;
```

يجب أن يكون الناتج:

```
 server_encoding
-----------------
 UTF8

 client_encoding
-----------------
 UTF8
```

---

## 🚀 اختبار النظام

بعد تطبيق أي من الحلول أعلاه:

1. أعد تشغيل السيرفر:
```bash
npm run dev
```

2. جرّب رفع ملف ABX

3. يجب أن يعمل بدون أخطاء! ✅

---

## 📝 ملاحظات

- **الطريقة 1** هي الأسرع والأسهل
- **الطريقة 2** هي الحل الدائم
- **الطريقة 3** تتطلب حذف البيانات الموجودة

---

**التاريخ**: 21 يناير 2026
**الحالة**: ✅ تم تحديد المشكلة والحل
