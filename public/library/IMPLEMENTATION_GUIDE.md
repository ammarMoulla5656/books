# دليل التطبيق - المرحلة الأولى من التطوير

## ✅ ما تم إنجازه

### 1. إصلاح مشكلة عرض التصنيفات
**الملف:** `index.html`
- ✅ تم إصلاح عرض التصنيفات في شبكة 4 أعمدة بدلاً من عمود واحد
- ✅ إضافة media queries للأجهزة الصغيرة

### 2. قائمة السياق الذكية (Smart Context Menu)
**الملفات الجديدة:**
- `js/context-menu.js` - منطق القائمة
- `css/context-menu.css` - تصميم القائمة

**المزايا:**
- عند تحديد أي نص واضغط كليك يمين → تظهر قائمة
- 9 خيارات ذكية متاحة
- تأثيرات بصرية جميلة مع نجوم متوهجة لخيارات AI

---

## 🎯 كيفية الاستخدام

### استخدام قائمة السياق:

1. **اختر أي نص في الكتاب**
   ```
   روي عن الإمام الصادق عليه السلام أنه قال: "العقل ما عُبد به الرحمن"
   ```

2. **اضغط كليك يمين (Right Click)**
   ```
   قائمة سياق ستظهر بخيارات:
   ┌────────────────────────┐
   │  📋 نسخ                │
   │  ─────────────────────  │
   │  ⭐ شرح هذا النص       │
   │  ⭐ تلخيص             │
   │  ⭐ أين ورد؟          │
   │  ─────────────────────  │
   │  📖 جلب نص مشابه      │
   │  🔗 البحث في Google   │
   │  ─────────────────────  │
   │  📝 إضافة للتقرير     │
   │  🔖 إضافة علامة      │
   │  ⭐ مناقشة مع AI     │
   └────────────────────────┘
   ```

### خيارات القائمة:

#### 1. **نسخ** 📋
- ينسخ النص المحدد إلى الحافظة
- يظهر تنبيه: "تم النسخ بنجاح"

#### 2. **شرح هذا النص** ⭐ (AI)
- يفتح نافذة الشرح
- يعرض شرح مفصل للنص المحدد
- متصل بخدمة الذكاء الاصطناعي

#### 3. **تلخيص** ⭐ (AI)
- يلخص النص المحدد
- يعرض الملخص في نافذة منفصلة
- متصل بخدمة الذكاء الاصطناعي

#### 4. **أين ورد؟** ⭐ (AI)
```
عند اختيار هذا الخيار:
✓ يبحث عن النص في جميع الكتب
✓ يعرض قائمة بأماكن الورود
✓ يمكن فتح كل نتيجة مباشرة

مثال على النتائج:
─────────────────────────
📖 الكافي
  ├─ كتاب الحجة، باب 12
  └─ كتاب الإيمان، باب 5

📖 بحار الأنوار
  ├─ المجلد 23، ص 234
  └─ المجلد 35، ص 12
─────────────────────────
```

#### 5. **جلب نص مشابه** 📖
```
يفتح صندوق حوار يسأل:
"ماذا تريد أن أجلب لك؟"

أمثلة على الطلبات:
✓ "حديث الكساء"
✓ "دعاء كميل"
✓ "آية التطهير"
✓ "خطبة الغدير"
```

#### 6. **البحث في Google** 🔗
- يفتح Google بحثاً عن النص
- ينقلك إلى صفحة بحث جديدة

#### 7. **إضافة للتقرير** 📝
- إذا كان محرر التقارير مفتوح:
  - يضيف النص كاقتباس في التقرير
- إذا لم يكن مفتوح:
  - يظهر رسالة: "افتح محرر التقارير أولاً"

#### 8. **إضافة علامة** 🔖
- يحفظ النص كعلامة مرجعية
- يتم الحفظ في localStorage
- يمكن استرجاع العلامات لاحقاً

#### 9. **مناقشة مع AI** ⭐ (AI)
```
يفتح نافذة محادثة:
👤 أنت: "العقل ما عُبد به الرحمن"

🤖 المساعد:
هذا حديث شريف عن الإمام الصادق عليه السلام
يشير إلى أهمية العقل في العبادة...
[مع مصادر الحديث]
```

---

## 📁 الملفات المحدثة

### 1. `index.html`
```diff
+ <link rel="stylesheet" href="css/context-menu.css">
+ <script src="js/context-menu.js"></script>
```

### 2. `book.html`
```diff
+ <link rel="stylesheet" href="css/context-menu.css">
+ <script src="js/context-menu.js"></script>
```

### 3. `search.html`
```diff
+ <link rel="stylesheet" href="css/context-menu.css">
+ <script src="js/context-menu.js"></script>
```

### 4. `category.html`
```diff
+ <link rel="stylesheet" href="css/context-menu.css">
+ <script src="js/context-menu.js"></script>
```

---

## 🛠️ الملفات الجديدة

### `js/context-menu.js`
**الحجم:** ~350 سطر
**الوظائف الرئيسية:**
- `init()` - تهيئة القائمة
- `createMenu()` - إنشاء عناصر القائمة
- `bindEvents()` - ربط أحداث الكليك
- `show(x, y)` - إظهار القائمة بموضع معين
- `hide()` - إخفاء القائمة
- `handleAction(action)` - معالجة الإجراءات
- دوال خاصة لكل خيار

### `css/context-menu.css`
**الحجم:** ~350 سطر
**التأثيرات:**
- تأثير الظهور والاختفاء السلس
- نجوم متوهجة لخيارات AI
- تأثيرات hover جميلة
- responsive design للموبايل

---

## 🧪 الاختبار

### خطوات الاختبار:

1. **افتح الكتاب**
   ```
   انقر على أي كتاب → book.html?id=kafi
   ```

2. **حدد نصاً**
   ```
   اسحب الماوس على أي نص في الكتاب لتحديده
   ```

3. **اضغط كليك يمين**
   ```
   انقر بزر الماوس الأيمن على النص المحدد
   ```

4. **جرّب الخيارات**
   ```
   انقر على أي خيار من خيارات القائمة
   ```

### أمثلة للاختبار:

**مثال 1: نسخ نص**
- اختر: "العقل ما عُبد به الرحمن"
- انقر: "نسخ"
- النتيجة: رسالة "تم النسخ بنجاح"

**مثال 2: البحث عن نص**
- اختر: "عن الإمام الصادق"
- انقر: "أين ورد؟"
- النتيجة: قائمة بأماكن الورود

**مثال 3: البحث في Google**
- اختر: أي نص
- انقر: "البحث في Google"
- النتيجة: صفحة Google البحث تفتح

---

## 🎨 التصميم والتأثيرات

### تأثيرات خيارات AI ⭐

```css
/* نجم متوهج */
@keyframes starTwinkle {
    0%, 100% { text-shadow: none; }
    50% { text-shadow: 0 0 8px #0d7377; }
}

/* خط ملون على اليسار */
.ai-action::after {
    content: '';
    background: linear-gradient(to bottom, #0d7377, transparent);
    opacity: 0; /* يظهر عند hover */
}
```

### حركة القائمة

```css
/* تأثير الظهور */
.smart-context-menu.visible {
    transform: scale(1) translateY(0);
    opacity: 1;
    transition: all 0.15s ease;
}
```

---

## 📊 معلومات إضافية

### حجم الملفات:
```
js/context-menu.js  ─── 350 سطر ─── ~9 KB
css/context-menu.css  ─ 350 سطر ─── ~8 KB
```

### المتغيرات المستخدمة:
- `SmartContextMenu.menu` - عنصر القائمة
- `SmartContextMenu.selectedText` - النص المحدد
- `SmartContextMenu.menuPosition` - موضع القائمة

### التخزين المحلي:
```javascript
// العلامات المحفوظة
localStorage.getItem('text-bookmarks')
// [
//   {
//     text: "النص المحفوظ",
//     book: "kafi",
//     section: "ch1-s1",
//     timestamp: "2024-01-15T10:30:00Z"
//   }
// ]
```

---

## ⚙️ الإعدادات المتقدمة

### تغيير زمن الانتقال:
```javascript
transition: all var(--transition-fast); // 150ms
```

### تغيير ألوان خيارات AI:
```css
.context-menu-item.ai-action {
    color: var(--accent-primary); /* تغيير اللون */
}
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: القائمة لا تظهر
**الحل:**
```javascript
// تحقق من أن النص محدد بشكل صحيح
const selection = window.getSelection().toString().trim();
console.log('Selected text:', selection); // يجب أن يطبع النص
```

### المشكلة: الخيارات لا تعمل
**الحل:**
```javascript
// تحقق من أن AIFeatures محمل
console.log('AIFeatures:', window.AIFeatures);
// يجب أن يطبع كائن AIFeatures
```

---

## 📝 ملاحظات مهمة

1. **التوافقية:**
   - تعمل على جميع المتصفحات الحديثة
   - تدعم الموبايل والتابلت

2. **الأداء:**
   - لا تؤثر على أداء الصفحة
   - استخدام event delegation للكفاءة

3. **الوصولية:**
   - دعم keyboard shortcuts
   - دعم قارئات الشاشة

---

## 🚀 الخطوات التالية

بعد اختبار هذه المرحلة، سننتقل إلى:
1. تحسين ميزات AI الحالية
2. إنشاء مركز الذكاء الاصطناعي
3. نظام التبويبات المتعددة

---

**آخر تحديث:** 2024-01-16
**الإصدار:** 2.0.1
**الحالة:** جاهز للاستخدام
